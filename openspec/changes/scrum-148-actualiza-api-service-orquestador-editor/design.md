## Context

- Jira issue key: SCRUM-148
- Jira summary: ACTUALIZA-API-SERVICE-ORQUESTADOR-EDITOR-TIPTAP
- Jira URL: https://contasoftcompany.atlassian.net/browse/SCRUM-148

## Context Reference

- openspec/context/multi-repo-context.md
- openspec/context/OPSXJ_BACKEND_RULES.md

## Problem Statement

PROMPT ARQUITECTÓNICO — Ticket BE (FINAL / COPY-PASTE) Actualizar API + Service Orquestador Full Save para incluir catálogo de contextos usando ContextCode y context_definition_id Rol esperado Arquitecto backend senior (.NET, Core, Dapper, transacciones, consistencia de datos, pruebas y documentación). ——— OBJETIVO Actualizar el Full Save del editor Tiptap para que el guardado completo sea atómico e incluya contexto de negocio basado en catálogo: Guardar/actualizar documento HTML (ra_editor_documents) Resolver definición activa desde catálogo (ra_editor_context_definitions) usando ContextCode Registrar relación documento↔contexto usando context_definition_id y entity_id (ra_editor_document_context) Sincronizar imágenes (ra_editor_document_image_links + ra_editor_document_images) COMMIT solo si todo es exitoso; ROLLBACK si cualquier paso falla ——— PRECONDICIONES (CRÍTICAS) No crear repositorio nuevo para Full Save. Reutilizar repos existentes y todos deben soportar conn/trans externas: IGuardaEditorDocumentRepository ISolicitaEditorContextDefinitionRepository IGuardaEditorDocumentContextRepository ISincronizaEditorDocumentImagesRepository Si alguno no soporta conn/trans, este ticket queda bloqueado hasta ajustarlo. ——— UBICACIÓN ESPERADA Controller: DocuArchi.Api/Controllers/GestorDocumental/Editor/FullSaveEditorDocumentController.cs Service: MiApp.Services/Service/GestorDocumental/Editor/ServiceFullSaveEditorDocument.cs DTO: MiApp.DTOs/DTOs/GestorDocumental/Editor/ Docs: Docs/GestorDocumental/Editor/ Repositorios reutilizados: MiApp.Repository/Repositorio/GestorDocumental/Editor/GuardaEditorDocumentRepository.cs MiApp.Repository/Repositorio/GestorDocumental/Editor/SolicitaEditorContextDefinitionRepository.cs MiApp.Repository/Repositorio/GestorDocumental/Editor/GuardaEditorDocumentContextRepository.cs MiApp.Repository/Repositorio/GestorDocumental/Editor/SincronizaEditorDocumentImagesRepository.cs ——— CONTRATO OFICIAL (REQUEST ACTUALIZADO) public sealed class FullSaveEditorDocumentRequestDto { public long? DocumentId { get; set; } public long? TemplateId { get; set; } public int? TemplateVersion { get; set; } public string? DocumentTitle { get; set; } public string? DocumentHtml { get; set; } public string? StatusCode { get; set; } public string? CreatedBy { get; set; } public string? UpdatedBy { get; set; } public string ContextCode { get; set; } = null!;
  public long EntityId { get; set; }

  public List<string> ImageUids { get; set; } = new(); } Response: AppResponses<RaEditorDocument?> ——— RUTA OFICIAL POST /api/gestor-documental/editor/document/full-save ——— REGLAS DE SEGURIDAD Y VALIDACIONES (OBLIGATORIAS) Controller: Validar claim defaulalias Validar request no nulo Validar DocumentHtml requerido (no null/empty/whitespace) Validar ContextCode requerido (no null/empty/whitespace) Validar EntityId > 0 Validar ImageUids no nulo (puede ser vacío) Delegar al service; success=false → BadRequest, success=true → Ok Service: Validar defaultDbAlias requerido y Trim() Normalizar ContextCode = ContextCode.Trim().ToUpperInvariant() (o regla equivalente definida por el proyecto) Normalizar ImageUids: Trim + remover vacíos + Distinct(StringComparer.OrdinalIgnoreCase) ——— IMPLEMENTACIÓN — CONTROLLER Restricciones: No transacciones en controller No catálogo en controller No SQL/joins en controller ——— IMPLEMENTACIÓN — SERVICE (ORQUESTADOR REAL) Interface: public interface IServiceFullSaveEditorDocument { Task<AppResponses<RaEditorDocument?>> FullSaveAsync( FullSaveEditorDocumentRequestDto request, string defaultDbAlias ); } Reglas: El service abre conn y trans (una sola vez) El service NO ejecuta SQL directo El service orquesta y pasa conn/trans a repos Rollback obligatorio ante cualquier error o excepción ——— TRANSACCIÓN (FLUJO OBLIGATORIO) BEGIN TRANSACTION Guardar/actualizar documento (repo documento) → obtener DocumentId Resolver definición activa (repo catálogo) por ContextCode Si no existe o está inactiva → success=false + rollback Guardar relación contexto (repo contexto) con: document_id context_definition_id entity_id (idempotente si ya existe activa) Validar reglas del catálogo si aplican (mínimo requires_unique_entity) Sincronizar imágenes (repo imágenes) con lista normalizada Validar consistencia final (mínimo: todos los pasos success=true y DocumentId>0) COMMIT Si falla cualquier paso: ROLLBACK y retornar error controlado (data=null) ——— SEMÁNTICA DE RESPUESTA Éxito total: success=true, data!=null, errors=[] Error validación: success=false, data=null, errors Validation Error catálogo (ContextCode inexistente/inactivo): success=false, rollback, data=null Error documento/contexto/imágenes: success=false, rollback, data=null Excepción: success=false, rollback, data=null, errors Exception ——— INFRAESTRUCTURA DE DATOS (PARA SER COMPATIBLE CON EDITOR) Usar infraestructura existente del módulo Editor: Dapper + IDbConnectionFactory (y/o engine del proyecto si aplica). No concatenar SQL manualmente (solo parametrizado). ——— PRUEBAS OBLIGATORIAS (MÍNIMAS) Controller unit: claim faltante → 400 request inválido → 400 service ok → 200 service error → 400 Service unit (mock repos): success total → invoca repos en orden y commit falla documento → rollback y no continúa ContextCode inexistente/inactivo → rollback y no guarda contexto/imágenes falla contexto → rollback y no sincroniza imágenes falla imágenes → rollback excepción → rollback Integración (preferida; si no Docker, Skip explícito): persistencia real en ra_editor_documents lectura real de ra_editor_context_definitions persistencia real en ra_editor_document_context persistencia real en ra_editor_document_image_links rollback real cuando falla uno de los pasos ——— DOCUMENTACIÓN OBLIGATORIA (Docs/GestorDocumental/Editor/) SCRUM-[ID]-Integracion-Frontend-FullSaveEditorDocument.md SCRUM-[ID]-Arquitectura-FullSaveEditorDocument.md SCRUM-[ID]-Implementacion-Detallada-FullSaveEditorDocument.md SCRUM-[ID]-Pruebas-FullSaveEditorDocument.md Actualizar docs relacionadas para reflejar: uso de ContextCode resolución interna de context_definition_id atomicidad documento + contexto + imágenes ——— CRITERIOS DE ACEPTACIÓN Endpoint Full Save actualizado con ContextCode y EntityId Resuelve catálogo dentro de la misma transacción Persiste relación con context_definition_id Sincroniza imágenes en la misma transacción Rollback ante cualquier falla (sin estados parciales) AppResponses<RaEditorDocument?> consistente Pruebas mínimas (unit + integración o Skip explícito) Documentación completa y actualizada

## Approach

- Convertir requerimientos del issue en deltas OpenSpec claros y testeables.
- Aplicar restricciones de repositorio, arquitectura y pruebas de OPSXJ_BACKEND_RULES.
- Definir alcance y no-alcance antes de implementar.
- Validar con openspec.cmd validate scrum-148-actualiza-api-service-orquestador-editor.