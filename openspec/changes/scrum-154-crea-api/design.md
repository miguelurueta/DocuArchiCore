## Context

- Jira issue key: SCRUM-154
- Jira summary: CREA-API-
- Jira URL: https://contasoftcompany.atlassian.net/browse/SCRUM-154

## Context Reference

- openspec/context/multi-repo-context.md
- openspec/context/OPSXJ_BACKEND_RULES.md

## Problem Statement

• # PROMPT ARQUITECTÓNICO — Ticket BE (Editor) Resolver carga inicial vs documento existente (ResolveEditorDocument) Rol esperado: Arquitecto de software senior backend (.NET, C#, Core, Clean Architecture, services, repositorios, contratos API, seguridad por claims, pruebas y documentación técnica). ——— OBJETIVO Centralizar en backend la lógica de resolución de contenido del editor: Si existe documento → devolver documento (mode=existing) Si no existe → devolver contenido inicial (mode=initial) Eliminar completamente la lógica duplicada en frontend y reducir llamadas innecesarias. ——— CONTEXTO EXISTENTE APIs actuales: GET /api/gestor-documental/editor/initial-content GET /api/gestor-documental/editor/document/by-context GET /api/gestor-documental/editor/document/{documentId} Problema actual: frontend decide qué endpoint llamar lógica duplicada en frontend múltiples llamadas innecesarias reglas de negocio distribuidas Controller existente relevante: DocuArchi.Api\Controllers\GestorDocumental\Editor\SolicitaEditorDocumentByContextController.cs (implementa GET /document/by-context) ——— NUEVA API OFICIAL GET /api/gestor-documental/editor/document/resolve Query params: contextCode (string, requerido) entityId (long, requerido) templateDefinitionId (long, opcional) templateCode (string, opcional) prefer (string, opcional) → valores: existing (default) initial Regla adicional obligatoria: Si prefer tiene un valor distinto a existing|initial → 400 Validation (campo prefer). ——— CONTRATO DE RESPUESTA AppResponses<EditorResolveDocumentResponseDto> EditorResolveDocumentResponseDto: mode (string): existing | initial contextCode (string) entityId (long) documentId (long?) templateDefinitionId (long) templateCode (string) html (string) images (List) (tipo concreto a definir según DTOs actuales; ej. List<EditorImageDto> o List<string> uids) tokensResueltos (Dictionary<string,string?>?) Notas de contrato: En mode=existing: tokensResueltos = null. En mode=initial: documentId = null y images = []. ——— REGLAS DE NEGOCIO (CERRADAS — NO AMBIGUAS) VALIDACIONES contextCode null/empty → 400 entityId <= 0 → 400 contextCode inválido o inactivo → 400 (NO usar 404 en este ticket) Estandarizar el error: Type = "Validation" Field: contextCode / entityId / prefer según corresponda Message claro y accionable ——— RESOLUCIÓN DEFAULT (prefer=existing) Consultar documento existente (vía servicio existente): IServiceSolicitaEditorDocumentByContext CASOS: A) EXISTE documento: → retornar: mode=existing documentId html images tokensResueltos = null templateDefinitionId / templateCode: retornar si están disponibles en el documento/metadata; si no existen en el documento, se permite resolverlos sin costo alto (sin duplicar lógica pesada) o retornar valores por defecto definidos. B) NO EXISTE documento: → llamar: IServiceInitialContentEditor (GetInitialContentAsync con overrides) → retornar: mode=initial documentId = null html tokensResueltos images = [] ——— RESOLUCIÓN FORZADA (prefer=initial) CASOS: A) EXISTE documento: SI contexto NO permite múltiples: → retornar 409 Conflict SI contexto SÍ permite múltiples: → retornar initial (crear nuevo) → mode=initial B) NO EXISTE documento: → retornar initial normal Regla obligatoria: La verificación “permite múltiples” debe basarse en la definición de contexto (ra_editor_context_definitions) y/o la regla vigente del módulo (no inventar heurísticas en controller). ——— REGLA CRÍTICA — MÚLTIPLES DOCUMENTOS Si existen múltiples documentos y NO hay criterio determinístico formal: → retornar 409 Conflict NO implementar lógica de “último activo” en este ticket. Requisito técnico: Debe existir una forma real de detectar 0/1/muchos documentos. Si el servicio actual by-context no distingue “muchos”, este ticket incluye crear/ajustar un método/repositorio para obtener conteo/listado mínimo o un resultado discriminado. ——— PRIORIDAD DE PLANTILLA (OVERRIDE) Orden de resolución: templateDefinitionId (mayor prioridad) templateCode reglas del contexto (default) ——— IMPLEMENTACIÓN — CONTROLLER Ubicación: DocuArchi.Api/Controllers/GestorDocumental/Editor/ Decisión de diseño (alineada al repo): En este proyecto los controllers están separados por caso de uso; por consistencia se recomienda crear un controller nuevo: ResolveEditorDocumentController.cs Alternativa aceptada si el equipo lo exige: agregar la acción al controller existente, pero debe justificarse en PR. Acción: [HttpGet("document/resolve")] Inyección: IClaimValidationService IServiceResolveEditorDocument Flujo: validar claim defaulalias validar parámetros de request (contextCode, entityId, prefer válido) llamar service retornar: Ok(...) si success BadRequest(...) para validaciones y errores de negocio modelados como 400 Conflict(...) para 409 PROHIBIDO en controller: lógica de negocio (reglas de existencia, múltiples documentos, etc.) acceso a repositorio orquestar servicios múltiples “adivinar” plantilla Regla de validaciones (aclaración importante): Controller valida requisitos del request. Service valida consistencia de negocio (contexto válido/inactivo, múltiples docs, restricciones al prefer=initial, etc.). No duplicar la misma validación textual en ambos; separar responsabilidades. ——— IMPLEMENTACIÓN — SERVICE Interface: IServiceResolveEditorDocument Clase: ServiceResolveEditorDocument Método: Task<AppResponses<EditorResolveDocumentResponseDto>> ResolveAsync( string contextCode, long entityId, long? templateDefinitionId, string? templateCode, string prefer, string alias ); Reglas: Orquestar servicios existentes (IServiceSolicitaEditorDocumentByContext, IServiceInitialContentEditor) No duplicar lógica de repositorios No resolver claims (alias llega validado) try/catch obligatorio: No ocultar errores: mapear a AppResponses consistente (success=false, message estándar, errors si aplica) Si prefer inválido (por seguridad defensiva) debe retornar Validation 400-equivalente (aunque controller lo valide, el service debe ser robusto si se usa internamente). ——— SEGURIDAD Validar claim defaulalias en controller Service recibe alias ya validado NO reinterpretar identidad en service ——— PRUEBAS OBLIGATORIAS UNITARIAS existing → retorna documento no existe → retorna initial prefer=initial + existe + no permitido → 409 prefer=initial + permitido → initial contextCode inválido/inactivo → error (400) entityId inválido → error (400) múltiples documentos → 409 prefer inválido → 400 (Validation) INTEGRACIÓN flujo completo controller → service → repos datos reales coherentes contratos correctos QT / PUNTA A PUNTA llamada única resuelve correctamente modo frontend recibe datos consistentes no hay doble llamada tiempos de respuesta correctos consistencia de html renderizable comportamiento correcto en conflictos REGRESIÓN no romper endpoints existentes no alterar contratos existentes ——— VALIDACIÓN SOLID (OBLIGATORIA) SRP: controller/service/repos separados OCP: extensible sin romper contrato LSP: interfaces sustituibles ISP: interfaces específicas DIP: dependencias por interfaces Documentar resultados. ——— DEUDA TÉCNICA (OBLIGATORIO) Evaluar y documentar: duplicación lógica anterior en frontend ambigüedad previa de múltiples documentos falta de contrato unificado posibles mejoras futuras (ej. endpoint de listado para selección de documento cuando múltiples) Clasificar: crítica media menor ——— DOCUMENTACIÓN OBLIGATORIA Ruta: D:\imagenesda\GestorDocumental\DocuArchiCore\DocuArchiCore\Docs\GestorDocumental\Editor\ Archivos: SCRUM-XXX-Integracion-Frontend.md endpoint parámetros response ejemplos manejo de mode manejo de 409 (qué debe hacer UI) SCRUM-XXX-Arquitectura.md diagramas: clases secuencia casos de uso estado decisiones arquitectónicas SOLID por qué backend resuelve (single source of truth) SCRUM-XXX-Implementacion-Detallada.md paso a paso técnico archivos creados lógica aplicada decisiones contratos y mapeos de errores SCRUM-XXX-Pruebas.md unitarias integración QT cobertura METADATA DEL TICKET autor fecha contexto ——— CRITERIOS DE ACEPTACIÓN endpoint único funcional (/document/resolve) elimina lógica frontend duplicada maneja existing vs initial correctamente maneja conflictos (409) no hay ambigüedad en múltiples documentos (devuelve 409) validaciones 400 consistentes (incluye prefer inválido) pruebas completas documentación completa arquitectura respetada › Explain this codebase

## Approach

- Convertir requerimientos del issue en deltas OpenSpec claros y testeables.
- Aplicar restricciones de repositorio, arquitectura y pruebas de OPSXJ_BACKEND_RULES.
- Definir alcance y no-alcance antes de implementar.
- Validar con openspec.cmd validate scrum-154-crea-api.