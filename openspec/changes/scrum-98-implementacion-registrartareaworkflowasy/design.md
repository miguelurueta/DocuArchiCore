## Context

- Jira issue key: SCRUM-98
- Jira summary: IMPLEMENTACION-RegistrarTareaWorkflowAsync
- Jira URL: https://contasoftcompany.atlassian.net/browse/SCRUM-98

## Context Reference

- openspec/context/multi-repo-context.md
- openspec/context/OPSXJ_BACKEND_RULES.md

## Problem Statement

PROMPT ARQUITECTÓNICO — Implementación del repositorio RegistrarTareaWorkflowAsync y actualización del flujo de RegistrarRadicacionEntranteAsync Rol esperado: Arquitecto de software senior y desarrollador backend .NET OBJETIVO Implementar e integrar el repositorio RegistrarTareaWorkflowAsync dentro del flujo del servicio RegistrarRadicacionEntranteAsync con el objetivo de registrar una tarea workflow cuando no exista previamente una tarea asociada para la ruta workflow correspondiente. La implementación debe respetar la arquitectura actual del proyecto, el patrón Service → Repository y el uso de AppResponses como wrapper estándar. ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ DATOS DEL CASO ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Proceso principal: RegistrarRadicacionEntranteAsync Repositorio a consumir: RegistrarTareaWorkflowAsync Parámetros de entrada: idRuta → RutasWorkflow.id_Ruta nombreRuta → RutasWorkflow.Nombre_Ruta idGabinete → citaEstructuraTipoDoEntrante.codigo_gabinete_workflow idImagen → null idActividadWorkflow → GruposWorkflow.id_Actividad idUsuarioWorkflow → UsuarioWorkflow.idU_suario idFlujoTrabajo → requestCanonico.RE_flujo_trabajo?.id_tipo_flujo_workflow idActiovidadFujoTrabajo → actividadInicioFlujo.IdActividadFlujoTrabajo idUsuarioWorkflowFlujoTrabajo → actividadInicioFlujo.IdUsuarioWorkflowFlujoTrabajo estadoActividaModuloRad → 0 estadoModuloRadicado → citaEstructuraTipoDoEntrante.activo_modulo_respuesta estadoRecuperacionFlujoTrabajo → 0 IReadOnlyCollection<RelacionCamposRutaWorklflowDto> relaciones fecha actual del servidor defaultDbAlias → claims (defaulaliaswf) ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ CONDICIONES DEL FLUJO ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Para ejecutar el repositorio RegistrarTareaWorkflowAsync dentro del flujo debe cumplirse la condición: existenciaWorkflowResult.IdTareaWorkflow == 0 La llamada a RegistrarTareaWorkflowAsync debe ejecutarse después de la ejecución de ConsultarExistenciaRadicadoRutaWorkflowAsync. El resultado de RegistrarTareaWorkflowAsync no debe exponerse como respuesta pública independiente del servicio RegistrarRadicacionEntranteAsync. El resultado de RegistrarTareaWorkflowAsync debe utilizarse únicamente como dato interno dentro del flujo del servicio. Se debe implementar una variable local LocalRegistroTareaWorkflowResultDto interna de tipo: RegistroTareaWorkflowResultDto para almacenar el resultado del repositorio. Si RegistroTareaWorkflowResultDto no contiene un resultado válido, el flujo del servicio debe interrumpirse con un mensaje claro indicando que no se pudo registrar la tarea workflow. Si ConsultarExistenciaRadicadoRutaWorkflowAsync retorna error técnico o Success = false, el flujo no debe continuar. Si existenciaWorkflowResult.IdTareaWorkflow != 0, no debe ejecutarse RegistrarTareaWorkflowAsync y el flujo debe continuar con la lógica ya definida para ese escenario. La variable local LocalRegistroTareaWorkflowResultDto debe quedar por fuera de la condicion existenciaWorkflowResult.IdTareaWorkflow == 0. ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ ARQUITECTURA APLICABLE ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ El flujo debe mantenerse así: RegistrarRadicacionEntranteAsync (Service) ↓ ConsultarExistenciaRadicadoRutaWorkflowAsync ↓ si IdTareaWorkflow == 0 ↓ RegistrarTareaWorkflowAsync (Repository) ↓ uso interno del resultado dentro del Service ↓ continuar con el flujo de radicación Reglas: El Service consume el Repository El Repository retorna datos al Service El resultado se usa internamente y no se expone como respuesta pública independiente No colocar lógica de negocio en Repository ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ ALCANCE ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ La actualización debe incluir, según impacto real: integración de RegistrarTareaWorkflowAsync dentro de RegistrarRadicacionEntranteAsync ajuste del flujo con base en existenciaWorkflowResult repositorios impactados servicios impactados modelos de MiApp.Models relacionados DTOs de entrada/salida impactados pruebas unitarias pruebas de integración documentación técnica ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ REQUERIMIENTO PRINCIPAL ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ La solución debe: consumir RegistrarTareaWorkflowAsync únicamente cuando no exista tarea workflow previa usar el resultado del repositorio como variable interna del flujo interrumpir el flujo si no se logra registrar la tarea workflow ajustar DTOs impactados si realmente aplica ajustar la lógica del servicio si es necesario mantener compatibilidad con la arquitectura existente no introducir cambios innecesarios fuera del alcance ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ CRITERIOS TÉCNICOS ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Mantener AppResponses<T> como wrapper estándar de respuesta Toda función modificada debe quedar envuelta en try/catch No usar SQL concatenado manualmente Toda consulta debe ser parametrizada para evitar SQL Injection Mantener consistencia con DapperCrudEngine y QueryOptions si ya forman parte del patrón actual del proyecto Agregar o actualizar comentarios XML en todas las funciones impactadas En caso de excepción, retornar success = false con el detalle técnico controlado según el estándar actual del proyecto ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ VALIDACIONES FUNCIONALES ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ La actualización debe validar: respuesta exitosa con datos válidos respuesta exitosa sin resultados manejo de excepción controlada coherencia entre modelo, DTO y datos reales de la tabla compatibilidad con el consumo actual desde capas superiores correcta ejecución condicional según existenciaWorkflowResult.IdTareaWorkflow interrupción del flujo cuando no se pueda registrar la tarea workflow ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ DOCUMENTACIÓN TÉCNICA ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Ubicación: /Docs/Radicacion/Tramite Documentar: consumo del repositorio RegistrarTareaWorkflowAsync condición de ejecución basada en existenciaWorkflowResult uso interno de RegistroTareaWorkflowResultDto parámetros funcionales utilizados comportamiento cuando ya existe tarea workflow comportamiento cuando el registro falla ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ PRUEBAS ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ PRUEBAS UNITARIAS Incluir pruebas para: respuesta exitosa con datos respuesta exitosa sin resultados excepción controlada ejecución de RegistrarTareaWorkflowAsync cuando IdTareaWorkflow == 0 no ejecución de RegistrarTareaWorkflowAsync cuando IdTareaWorkflow != 0 fallo en el registro de tarea workflow mocks de repositorio, servicio o dependencias según corresponda PRUEBAS DE INTEGRACIÓN Usar: MySQL Testcontainers / Docker Validar: correspondencia correcta del mapeo comportamiento con registros válidos comportamiento sin registros correcta integración del flujo de existencia + registro de tarea workflow ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ ENTREGABLES ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Funciones impactadas actualizadas Modelos impactados actualizados DTOs impactados actualizados Servicio ajustado según corresponda Comentarios XML completos Pruebas unitarias Pruebas de integración Documentación técnica en /Docs/Radicacion/Tramite ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ RESTRICCIONES ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ No romper la arquitectura actual del proyecto No usar SQL concatenado manualmente Mantener consistencia con los patrones existentes Usar AppResponses como wrapper estándar No modificar contratos no impactados sin justificación técnica

## Approach

- Convertir requerimientos del issue en deltas OpenSpec claros y testeables.
- Aplicar restricciones de repositorio, arquitectura y pruebas de OPSXJ_BACKEND_RULES.
- Definir alcance y no-alcance antes de implementar.
- Validar con openspec.cmd validate scrum-98-implementacion-registrartareaworkflowasy.