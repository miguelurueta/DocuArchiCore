## Context

- Jira issue key: SCRUM-155
- Jira summary: CORRECCION-INCONSISTENCIA-CONTRATUAL-SOLICITA-RESPEUSTA-ID-TAREA-WOKFLOW
- Jira URL: https://contasoftcompany.atlassian.net/browse/SCRUM-155

## Context Reference

- openspec/context/multi-repo-context.md
- openspec/context/OPSXJ_BACKEND_RULES.md

## Problem Statement

PROMPT ARQUITECTÓNICO — SCRUMCORE-144 (VERSIÓN ACTUALIZADA / COPY-PASTE) Corregir inconsistencia contractual de estructura de respuesta por idTareaWf y eliminar flicker en Gestión Respuesta Rol esperado: Arquitecto de software senior backend/frontend (React, .NET, contratos API, consistencia de estado, observabilidad, UX de estados, pruebas y documentación técnica). ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ OBJETIVO ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Corregir el comportamiento inconsistente del flujo de carga de Gestión Respuesta para que, al consultar la estructura de respuesta por idTareaWf, el backend entregue un estado contractual estable y el frontend pueda renderizar sin flicker. El objetivo es eliminar el caso en el que el UI entra en estado bloqueado por “Sin resultados” y luego, sin interacción del usuario, renderiza el detalle operativo de GestionRespuesta. ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ RUTA / ENDPOINT CANÓNICO (OBLIGATORIO) ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ La ruta canónica según el Controller existente es: GET /api/GestionCorrespondencia/solicita-estructura-respuesta-id-tarea?idTareaWf={idTareaWf} Fuente en código: Controller: DocuArchi.Api\Controllers\GestionCorrespondencia\SolicitaEstructuraRespuestaIdTareaController.cs [Route("api/GestionCorrespondencia")] + [HttpGet("solicita-estructura-respuesta-id-tarea")] NOTA DE COMPATIBILIDAD (IMPORTANTE): Existe documentación previa que menciona /api/gestor-documental/solicita-estructura-respuesta-id-tarea. No se deben eliminar rutas ni romper consumidores: si hoy existe un alias/ruta alternativa en el sistema, debe mantenerse. Si no existe, se debe actualizar la documentación/consumo para que use la ruta canónica. ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ PROBLEMA ACTUAL ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Al abrir la ruta: /dashboard/gestion-correspondencia/respuesta/:id el frontend muestra primero el estado: “Gestión respuesta bloqueada” y luego renderiza el contenido master-detail de GestionRespuesta. Esto ocurre porque el endpoint: GET /api/GestionCorrespondencia/solicita-estructura-respuesta-id-tarea?idTareaWf=934 puede responder, para el mismo idTareaWf, primero: success = true message = "Sin resultados" data = [] y luego: success = true message = "YES" data = [ ... ] Eso rompe la semántica de negocio y genera flicker. ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ REGLA DE NEGOCIO (OBLIGATORIA) ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Debe existir una única semántica contractual clara para ausencia de datos. DECISIÓN ARQUITECTÓNICA OBLIGATORIA: El backend NO puede usar el mismo estado funcional para dos realidades distintas: ausencia definitiva de datos datos aún no disponibles / consistencia eventual / procesamiento Por lo tanto, el contrato debe distinguir explícitamente entre: success empty pending REGLA: empty = ausencia definitiva pending = todavía puede aparecer información después (procesamiento/consistencia eventual) success = existen datos utilizables PROHIBIDO: usar "Sin resultados" como estado definitivo si aún existe posibilidad de transición posterior a "YES" sin una semántica explícita intermedia. ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ DECISIÓN CONTRACTUAL (OBLIGATORIA, NO REGRESIVA) ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ La respuesta del endpoint debe incluir meta.status o equivalente funcional. REQUERIMIENTO DE COMPATIBILIDAD (CRÍTICO PARA EVITAR REGRESIONES): El cambio debe ser ADITIVO (NO BREAKING): se mantiene el shape actual AppResponses<T> (success, message, data, meta, errors) y solo se agrega meta.status (y opcionales). No se eliminan campos, no se renombra nada, no se cambia el tipo de data. El contrato debe seguir siendo AppResponses<List<RaRespuestaRadicado>>. Ejemplo recomendado (vacío definitivo): { "success": true, "message": "Sin resultados", "data": [], "meta": { "status": "empty" }, "errors": [] } Estados permitidos: success empty pending Semántica canónica (la UI NO decide por message): success data contiene elementos frontend renderiza GestionRespuesta (master-detail) empty ausencia definitiva frontend bloquea (estado definitivo) no debe renderizar detalle luego pending aún no es definitivo frontend muestra skeleton / loading estable (sin bloqueo definitivo) no debe mostrar “Sin resultados” como bloqueo definitivo REGLA DE FALLBACK PARA CLIENTES NO MIGRADOS (OBLIGATORIA): Si meta.status NO está presente (backend viejo), el cliente aplica la semántica legacy: success=true y data.length>0 => success success=true y data.length==0 => empty (legacy) Esto evita romper releases desincronizados FE/BE. IMPORTANTE (ANTI “LOADING INFINITO”): pending no puede ser arbitrario: solo se puede devolver si el backend tiene una señal objetiva de procesamiento/consistencia eventual. Si no existe señal objetiva, el backend debe devolver empty cuando no hay filas (estado definitivo). Opcional recomendado: meta.retryAfterMs (solo como hint; NO implica introducir polling nuevo si no existe hoy). ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ CONTEXTO EXISTENTE ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Backend: DocuArchi.Api / GestionCorrespondencia Controller: DocuArchi.Api\Controllers\GestionCorrespondencia\SolicitaEstructuraRespuestaIdTareaController.cs Service: MiApp.Services\Service\GestorDocumental\ServiceSolicitaEstructuraRespuesta.cs Repository: MiApp.Repository\Repositorio\GestionCorrespondencia\SolicitaEstructuraRespuestaIdTareaRepository.cs Frontend: DocuArchiCore.react Hook: src/modules/gestionCorrespondencia/hooks/useEstructuraRespuestaIdTarea.ts Ruta: src/modules/gestionCorrespondencia/routes/GestionCorrespondenciaRoute.tsx Caso reproducible: idTareaWf = 934 ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ ALCANCE ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Backend: instrumentar el endpoint para detectar inconsistencia real corregir la semántica de respuesta definir estado contractual explícito (meta.status) asegurar consistencia entre llamadas repetidas corregir query para incluir TramiteDocumento si aplica garantizar que pending solo exista con señal objetiva (si no, no usarlo) Frontend: consumir el nuevo estado contractual bloquear solo cuando el estado sea definitivo (empty) mostrar skeleton / pending si el backend indica pending evitar render ambiguo del master-detail implementar fallback cuando meta.status no exista (compatibilidad) Documentación: documentar contrato nuevo (aditivo) documentar semántica de estados documentar fallback legacy documentar pruebas y trazabilidad ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ NO ALCANCE ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ no rediseñar todo el módulo GestionRespuesta no cambiar rutas del frontend no introducir polling nuevo salvo que el backend lo requiera explícitamente por contrato (y quede documentado) no rediseñar AppResponses completo del sistema no cambiar otros módulos no impactados no eliminar rutas existentes del API (si existen alias, se mantienen) ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ EVIDENCIA ACTUAL ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Caso sin resultados: { "success": true, "message": "Sin resultados", "data": [], "meta": null, "errors": [] } Caso con resultados (mismo idTareaWf=934): { "success": true, "message": "YES", "data": [ { "IdTareaWf": 934, "Radicado": "2500466700036", "Destinatario": "Carlos Mario Fuentes Luna", "TramiteDocumento": null } ], "meta": null, "errors": [] } ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ ANÁLISIS TÉCNICO (CAUSA PROBABLE) ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Posibles causas a validar: defaultDbAlias distinto entre requests consulta a distinta base / réplica / alias inserción tardía / consistencia eventual criterio no estable en el backend falta de selección de TramiteDocumento en query SQL IMPORTANTE: Antes de corregir frontend, el backend debe convertirse en fuente de verdad contractual estable. ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ REGLAS DE IMPLEMENTACIÓN — BACKEND ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Instrumentación obligatoria por request Agregar logs con: idTareaWf claim defaulalias SELECT DATABASE() (o equivalente) rows.Count timestamp de request X-Request-Id o correlation id duración de consulta resultado final (meta.status: success/empty/pending) Consistencia de origen Garantizar que consultas repetidas de corto intervalo: usen misma resolución de alias/base no alternen entre fuentes inconsistentes si hay balanceo/réplica, dejar trazabilidad suficiente para evidenciarlo Definir semántica estable (NO AMBIGUA) Si hay datos → meta.status = "success" Si no hay datos y la ausencia es definitiva → meta.status = "empty" Si no hay datos pero existe señal objetiva de “aún puede aparecer” → meta.status = "pending" REGLA CRÍTICA (ANTI-REGRESIÓN): Si data=[], el backend debe devolver meta.status coherente. Está prohibido devolver message="Sin resultados" como estado “definitivo” cuando en realidad es transitorio. Está prohibido devolver pending sin señal objetiva (para evitar loading infinito). Corregir query SQL Agregar explícitamente: rr.TRAMITE_DOCUMENTO AS TramiteDocumento si el campo existe en DB y debe exponerse. Prohibición contractual el backend no puede alternar para el mismo idTareaWf entre “vacío definitivo” y “con datos” sin que exista semántica explícita que explique el fenómeno (ej. pending con señal objetiva, o corrección de origen/alias) message no debe ser la única fuente de decisión; meta.status es canónica ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ REGLAS DE IMPLEMENTACIÓN — FRONTEND ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ El frontend debe reaccionar según el nuevo contrato (fuente canónica: meta.status): status = "success" → renderizar GestionRespuesta status = "empty" → mantener bloqueo definitivo → no renderizar detalle después → no “reintentar” mostrando flicker status = "pending" → mostrar skeleton / loading state estable → no mostrar bloqueo definitivo → no mostrar “Sin resultados” como definitivo FALLBACK OBLIGATORIO (compatibilidad): Si meta.status no viene (backend no migrado): success && data.length > 0 => success success && data.length == 0 => empty (legacy) PROHIBIDO: inferir por cuenta propia que message (“YES”, “Sin resultados”) define el estado inferir que data = [] siempre es definitivo sin revisar meta.status cuando exista introducir polling nuevo si no está requerido y documentado por el backend ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ REGLAS DE CONSISTENCIA ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ el mismo idTareaWf no debe alternar entre empty y success sin semántica explícita (idealmente: corrección de origen; excepcionalmente: pending con señal objetiva) el frontend no debe hacer doble interpretación del mismo estado message no debe ser la única fuente de decisión meta.status (cuando exista) debe ser la fuente canónica ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ PRUEBAS OBLIGATORIAS ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ PRUEBAS UNITARIAS BACKEND idTareaWf con datos → meta.status = "success" idTareaWf sin datos definitivos → meta.status = "empty" idTareaWf en procesamiento/eventual (solo si existe señal objetiva) → meta.status = "pending" query incluye TramiteDocumento cuando aplique logs incluyen correlación mínima requerida Compatibilidad: se mantiene la estructura AppResponses<T>; las pruebas existentes de shape no deben romperse. si hoy se valida message="Sin resultados", puede mantenerse como texto, pero la decisión se mueve a meta.status. PRUEBAS DE INTEGRACIÓN BACKEND llamadas repetidas en corto tiempo al mismo idTareaWf mantienen consistencia contractual mismo alias/base entre requests correlacionados (o trazabilidad clara si no) no alterna entre vacío definitivo y con datos sin semántica/diagnóstico TramiteDocumento llega poblado cuando exista en DB si sigue siendo null, queda documentado el motivo PRUEBAS FRONTEND status="empty" mantiene bloqueo sin flicker status="success" renderiza detalle directamente status="pending" muestra skeleton y no bloqueo definitivo fallback: ausencia de meta.status no rompe (usa legacy) no hay transición visual inconsistente PRUEBAS QT / PUNTA A PUNTA abrir /respuesta/934 verificar que no existe parpadeo entre bloqueo y detalle verificar que el estado visual coincide con el contrato backend verificar trazabilidad por request-id si aplica verificar comportamiento estable en recarga de página ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ RIESGOS A EVITAR ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ seguir usando “Sin resultados” como estado ambiguo arreglar solo frontend ocultando el síntoma no detectar inconsistencia de alias/base dejar TramiteDocumento fuera del SELECT introducir polling o retry sin contrato de estado y sin documentación dejar lógica dual frontend/backend usar pending sin señal objetiva (riesgo de loading infinito) ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ DOCUMENTACIÓN OBLIGATORIA ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Ruta sugerida: Docs/GestionCorrespondencia/ Archivos: SCRUMCORE-144-Arquitectura.md Debe incluir: diagrama de clases diagrama de secuencia diagrama de estados del flujo resolve/bloqueo decisiones contractuales validación SOLID nota de compatibilidad: “cambio aditivo” + fallback legacy SCRUMCORE-144-Implementacion-Detallada.md Debe incluir: qué se corrigió en backend qué se corrigió en frontend cómo se resolvió el estado contractual (meta.status) cambios en query SQL cambios en hooks/rutas criterio objetivo de pending (o decisión de no usarlo si no hay señal) SCRUMCORE-144-Integracion-Frontend.md Debe incluir: nuevo contrato de consumo semántica de status reglas de render: success empty pending fallback cuando meta.status no exista ejemplos de payload SCRUMCORE-144-Pruebas.md Debe incluir: unitarias integración QT cobertura de flicker casos límite (incluye ausencia de meta.status) SCRUMCORE-144-Observabilidad.md Debe incluir: logs agregados request-id / correlación alias/base capturada estrategia de diagnóstico cómo detectar alternancia por fuente/alias ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ CRITERIOS DE ACEPTACIÓN ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ no hay flicker en Gestión Respuesta una vez que el estado es empty definitivo, el UI no renderiza GestionRespuesta después si existe eventual consistency, el backend la expresa como pending (solo con señal objetiva) y no como “Sin resultados” respuestas repetidas para el mismo idTareaWf no alternan entre [] y [data] sin semántica/diagnóstico TramiteDocumento llega poblado cuando exista en DB o queda documentado por qué puede ser null backend y frontend quedan alineados con un contrato estable compatibilidad garantizada: clientes sin meta.status no se rompen (fallback legacy)

## Approach

- Convertir requerimientos del issue en deltas OpenSpec claros y testeables.
- Aplicar restricciones de repositorio, arquitectura y pruebas de OPSXJ_BACKEND_RULES.
- Definir alcance y no-alcance antes de implementar.
- Validar con openspec.cmd validate scrum-155-correccion-inconsistencia-contratual-sol.