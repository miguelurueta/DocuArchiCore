## Context

- Jira issue key: SCRUM-172
- Jira summary: CREA-FUNCION-WORKFLOW-STORAGE-ALMACENAMIENTO
- Jira URL: https://contasoftcompany.atlassian.net/browse/SCRUM-172

## Context Reference

- openspec/context/multi-repo-context.md
- openspec/context/OPSXJ_BACKEND_RULES.md

## Problem Statement

PROMPT ARQUITECTÓNICO — ORCHESTRATOR / ENGINE PROMPT 11 — WorkflowStorageLogRepository (ENTERPRISE CRÍTICO — COBERTURA LEGACY LOGDOCUARCHI) ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ ROL ESPERADO ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Arquitecto Master Backend .NET especialista en: Clean Architecture DapperCrudEngine QueryOptions logging transaccional de workflow trazabilidad documental migración legacy controlada seguridad de acceso a datos con DapperCrudEngine observabilidad pruebas unitarias e integración ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ OBJETIVO ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Implementar el repositorio transaccional encargado de registrar el log workflow equivalente al legacy logdocuarchi . Este componente debe: insertar registro en logdocuarchi cuando IdTareaWorkflow > 0 usar conexión y transacción existentes no abrir conexión no hacer commit no hacer rollback parametrizar SQL mantener trazabilidad con idAlmacen, gabinete, ruta lógica, radicado, tarea workflow y ruta workflow ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ CONTEXTO LEGACY CUBIERTO ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ El legacy inserta en logdocuarchi: id_tran = idal desc_op = "Registra" USER_OPER = useral DATE_TRANS = date1al RUT_DOCU = Ruta_Alamce_Image_ MODULO_REGISTRO = "WORKFLOW" GABINETE = _Nombre_Gabienete CAMPOS = fultex_log IP_TRANS = session ip_host_name HORA_REGISTRO = hora RADICADO = radicado ID_TAREA_WF = id_tarea_workflow ID_RUTA_WF = Id_Ruta_Workflow / id_ruta_workflow USER_PROPIETARIO = useral TIPOLOGIA_DOCUMENTAL = tipo_documento Solo se ejecuta si id_tarea_workflow <> 0. REFERENCIA DE TABLAS Ver: Docs/DataModel/StorageEngine-Tables.md Tablas usadas en este prompt: logdocuarchi ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ REGLA GLOBAL OBLIGATORIA — ACCESO A DATOS CON DAPPERCRUDENGINE ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Toda función de repositorio que realice consulta, inserción, actualización, eliminación, bloqueo lógico o lectura transaccional de datos DEBE usar obligatoriamente: DapperCrudEngine QueryOptions QueryFilter / filtros equivalentes del motor modelos tipados de entrada/salida contexto transaccional soportado por el DapperCrudEngine cuando aplique Ruta de referencia obligatoria: D:\imagenesda\GestorDocumental\DocuArchiCore\MiApp.Repository\Repositorio\DataAccess\DapperCrudEngine.cs Regla de arquitectura: Repository = DapperCrudEngine + QueryOptions PROHIBIDO en repositories: connection.ExecuteAsync(...) connection.QueryAsync(...) connection.ExecuteScalarAsync(...) SQL manual concatenado SQL interpolado con datos de usuario construir comandos SQL libres fuera del DapperCrudEngine abrir conexiones directamente abrir transacciones directamente hacer commit o rollback retornar AppResponses<T> resolver claims o lógica HTTP Permitido únicamente si el DapperCrudEngine ya lo soporta o se implementa primero como extensión centralizada: ejecución transaccional mediante DapperCrudEngine QueryOptions con TransactionContext QueryOptions con LockMode = ForUpdate QueryOptions con Insert/Update/Delete tipado QueryOptions con condición de concurrencia QueryOptions con retorno de identidad generada Si el DapperCrudEngine actual NO soporta alguna operación requerida por este prompt, se debe crear primero una extensión centralizada del DapperCrudEngine, documentada y probada, antes de implementar el repository. Queda prohibido saltarse el motor con Dapper directo. ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ REGLA TRANSACCIONAL CON DAPPERCRUDENGINE ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Cuando una operación de repository deba participar en una transacción del Storage Engine: El TransactionCoordinator abre la conexión/transacción o solicita al DapperCrudEngine un contexto transaccional. El repository NO manipula IDbConnection/IDbTransaction directamente salvo que el DapperCrudEngine exponga un contexto transaccional tipado. El repository recibe un contexto de ejecución transaccional, por ejemplo: StorageDbExecutionContext DapperCrudTransactionContext QueryExecutionContext Usar el nombre real disponible en el proyecto. Toda consulta, insert, update, lock o condición de concurrencia debe ejecutarse a través de DapperCrudEngine. Si se requiere SELECT ... FOR UPDATE, debe modelarse mediante QueryOptions.LockMode = ForUpdate o una propiedad equivalente. Si se requiere UPDATE con condición de concurrencia, debe modelarse en QueryOptions con filtros WHERE adicionales. Si se requiere INSERT y retorno de identidad, debe usarse método centralizado del DapperCrudEngine. Si no existe, implementarlo en el motor. ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ DEPENDENCIAS ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Depende de: Prompt 2 — contratos  SCRUMCORE-163 Prompt 6   extendido — TransactionCoordinator SCRUMCORE-167 Prompt 7 — Gabinete + Inventario SCRUMCORE-168 Prompt 9 — FileSystem para ruta final lógica si aplica SCRUMCORE-170 ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ UBICACIÓN ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Repository: MiApp.Repository/Repositorio/GestorDocumental/AlmacenamientoDocumental/Workflow/ Services/Builders si aplica: MiApp.Services/Service/GestorDocumental/AlmacenamientoDocumental/Workflow/ Documentación: Docs/GestorDocumental/AlmacenamientoDocumental/StorageEngine/ Archivos: IWorkflowStorageLogRepository.cs WorkflowStorageLogRepository.cs WorkflowStorageLogModel.cs IWorkflowStorageLogBuilder.cs WorkflowStorageLogBuilder.cs ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ MODELO ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ public sealed class WorkflowStorageLogModel { public long IdAlmacen { get; init; } public string DescripcionOperacion { get; init; } = "Registra";

public string UsuarioOperacion { get; init; } = string.Empty;

public DateTime FechaTransaccion { get; init; }

public string RutaDocumento { get; init; } = string.Empty;

public string ModuloRegistro { get; init; } = "WORKFLOW";

public string NombreGabinete { get; init; } = string.Empty;

public string Campos { get; init; } = string.Empty;

public string IpTransaccion { get; init; } = string.Empty;

public TimeSpan HoraRegistro { get; init; }

public string? Radicado { get; init; }

public long IdTareaWorkflow { get; init; }

public int IdRutaWorkflow { get; init; }

public string UsuarioPropietario { get; init; } = string.Empty;

public string? TipologiaDocumental { get; init; } } ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ INTERFACES ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ public interface IWorkflowStorageLogRepository { Task<int> InsertAsync( WorkflowStorageLogModel model, IDbConnection connection, IDbTransaction transaction ); } public interface IWorkflowStorageLogBuilder { WorkflowStorageLogModel Build( StorageContext context, StorageTransactionResult transactionResult ); } ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ REGLAS FUNCIONALES ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Si context.Command.Workflow == null → no insertar log. Si IdTareaWorkflow <= 0 → no insertar log. Si IdTareaWorkflow > 0 → insertar log. IdAlmacen debe venir de transactionResult.IdentityReservation.Identity.IdAlmacen. UsuarioOperacion debe venir de context.Usuario. UsuarioPropietario debe venir de context.Usuario. IdRutaWorkflow debe venir de context.Command.Workflow.IdRutaWorkflow. Radicado debe venir de context.Command.Inventario.Radicado si existe. Campos debe construirse con valores de CamposIndexacion, separados por "|", sin exponer datos sensibles si son marcados como sensibles en el futuro. RutaDocumento debe ser ruta lógica o relativa, no ruta física sensible completa, salvo compatibilidad legacy documentada. ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ AJUSTE ESPECÍFICO PROMPT 11 — LOGDOCUARCHI CON DAPPERCRUDENGINE ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ WorkflowStorageLogRepository debe reemplazar cualquier INSERT SQL manual por DapperCrudEngine + QueryOptions. Ejemplo conceptual: var options = new QueryOptions { TableName = "logdocuarchi", InsertValues = new Dictionary<string, object?> { ["id_tran"] = model.IdAlmacen, ["desc_op"] = model.DescripcionOperacion, ["USER_OPER"] = model.UsuarioOperacion, ["DATE_TRANS"] = model.FechaTransaccion, ["RUT_DOCU"] = model.RutaDocumento, ["MODULO_REGISTRO"] = model.ModuloRegistro, ["GABINETE"] = model.NombreGabinete, ["CAMPOS"] = model.Campos, ["IP_TRANS"] = model.IpTransaccion, ["HORA_REGISTRO"] = model.HoraRegistro, ["RADICADO"] = model.Radicado, ["ID_TAREA_WF"] = model.IdTareaWorkflow, ["ID_RUTA_WF"] = model.IdRutaWorkflow, ["USER_PROPIETARIO"] = model.UsuarioPropietario, ["TIPOLOGIA_DOCUMENTAL"] = model.TipologiaDocumental } }; var rows = await _DaperCrudEngine.InsertAsync(options, executionContext); if (rows != 1) throw new StorageTransactionException("No se insertó log workflow"); Si InsertValues o InsertAsync transaccional no existen, se deben agregar al DapperCrudEngine antes de implementar este prompt. ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ IMPLEMENTACIÓN — WorkflowStorageLogRepository ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Responsabilidad: validar modelo insertar en logdocuarchi usar SQL parametrizado usar conexión/transacción recibida no abrir conexión no hacer commit no rollback Validaciones: model != null connection != null transaction != null connection.State == Open IdAlmacen > 0 UsuarioOperacion requerido NombreGabinete requerido IdTareaWorkflow > 0 IdRutaWorkflow >= 0 FechaTransaccion != default SQL conceptual: INSERT INTO logdocuarchi ( id_tran, desc_op, USER_OPER, DATE_TRANS, RUT_DOCU, MODULO_REGISTRO, GABINETE, CAMPOS, IP_TRANS, HORA_REGISTRO, RADICADO, ID_TAREA_WF, ID_RUTA_WF, USER_PROPIETARIO, TIPOLOGIA_DOCUMENTAL ) VALUES ( @IdAlmacen, @DescripcionOperacion, @UsuarioOperacion, @FechaTransaccion, @RutaDocumento, @ModuloRegistro, @NombreGabinete, @Campos, @IpTransaccion, @HoraRegistro, @Radicado, @IdTareaWorkflow, @IdRutaWorkflow, @UsuarioPropietario, @TipologiaDocumental ); Si rows != 1 → StorageTransactionException. ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ IMPLEMENTACIÓN — WorkflowStorageLogBuilder ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Responsabilidad: construir WorkflowStorageLogModel no acceder a DB no acceder a filesystem no abrir transacción no insertar Debe mapear: IdAlmacen UsuarioOperacion FechaTransaccion NombreGabinete Campos Radicado IdTareaWorkflow IdRutaWorkflow UsuarioPropietario TipologiaDocumental Debe permitir inyección futura de: IIpAddressProvider IStorageRouteFormatter Si no existen, documentar fallback controlado. ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ INTEGRACIÓN EN PROMPT 6 SCRUMCORE-167 ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ En StorageTransactionCoordinator: if (context.Command.Workflow?.IdTareaWorkflow > 0) { var logModel = _workflowStorageLogBuilder.Build(context, txResult); await _workflowStorageLogRepository.InsertAsync(logModel, connection, transaction); workflowLogInserted = true; } Regla: si falla log workflow, rollback total. log workflow pertenece a la transacción DB. ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ NOTA DE PRECEDENCIA DAPPERCRUDENGINE ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Cualquier bloque de SQL conceptual, INSERT INTO, SELECT, UPDATE o ejemplo con connection.ExecuteAsync / ExecuteScalarAsync incluido en este prompt es únicamente referencia funcional legacy. La implementación real debe hacerse exclusivamente con DapperCrudEngine + QueryOptions. Si existe contradicción entre un ejemplo SQL y la regla DapperCrudEngine, prevalece DapperCrudEngine. ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ DEPENDENCY INJECTION ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ services.AddScoped<IWorkflowStorageLogRepository, WorkflowStorageLogRepository>(); services.AddScoped<IWorkflowStorageLogBuilder, WorkflowStorageLogBuilder>(); ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ OBSERVABILIDAD ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Logs obligatorios: INFO: workflow log build iniciado workflow log insertado WARNING: workflow informado sin IdTareaWorkflow válido IdRutaWorkflow no informado ERROR: error insertando log workflow Campos: requestId alias usuarioId idAlmacen idTareaWorkflow idRutaWorkflow nombreGabinete radicado fase = WorkflowLog NO loguear: FullText contenido completo de campos si contiene datos sensibles SQL con valores ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ PRUEBAS OBLIGATORIAS ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Unitarias — Builder: Workflow null → no construir / no aplica IdTareaWorkflow <= 0 → no aplica mapea IdAlmacen mapea usuario mapea radicado construye campos separados por "|" no incluye datos null de forma insegura Unitarias — Repository: model null → error connection null → error transaction null → error connection cerrada → error IdAlmacen <= 0 → error IdTareaWorkflow <= 0 → error Usuario vacío → error NombreGabinete vacío → error rows != 1 → error insert válido → success Integración: insert real en logdocuarchi rollback revierte log commit persiste log falla log → rollback total en coordinator QT: SQL parametrizado no SQL concatenado logs sin datos sensibles compatible con legacy Pruebas adicionales obligatorias por regla DapperCrudEngine: verificar que el repository invoca DapperCrudEngine y no IDbConnection directo. verificar que se construye QueryOptions con TableName correcto. verificar que se usan filtros parametrizados. verificar que LockMode = ForUpdate se usa cuando aplique. verificar que InsertValues/UpdateValues se usan cuando aplique. verificar que no existe ExecuteAsync/QueryAsync/ExecuteScalarAsync directo en el repository. verificar que no existe SQL concatenado. verificar que el contexto transaccional se propaga al DapperCrudEngine. ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ DOCUMENTACIÓN OBLIGATORIA ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Crear: SCRUM-[ID]-Arquitectura-WorkflowLog.md Debe incluir: objetivo relación con legacy logdocuarchi diagrama de clases diagrama de secuencia integración con TransactionCoordinator reglas transaccionales validación SOLID SCRUM-[ID]-Implementacion-Detallada-WorkflowLog.md Debe incluir: archivos creados rutas exactas modelo creado builder creado repository creado SQL conceptual mapping legacy → nuevo modelo DI decisiones técnicas SCRUM-[ID]-Pruebas-WorkflowLog.md Debe incluir: matriz unitarias matriz integración matriz QT rollback/commit casos cubiertos SCRUM-[ID]-Observabilidad-WorkflowLog.md Debe incluir: logs campos capturados troubleshooting ejemplos SCRUM-[ID]-Cobertura-Legacy-WorkflowLog.md Debe incluir tabla: id_tran → IdAlmacen desc_op → DescripcionOperacion USER_OPER → UsuarioOperacion DATE_TRANS → FechaTransaccion RUT_DOCU → RutaDocumento MODULO_REGISTRO → ModuloRegistro GABINETE → NombreGabinete CAMPOS → Campos IP_TRANS → IpTransaccion HORA_REGISTRO → HoraRegistro RADICADO → Radicado ID_TAREA_WF → IdTareaWorkflow ID_RUTA_WF → IdRutaWorkflow USER_PROPIETARIO → UsuarioPropietario TIPOLOGIA_DOCUMENTAL → TipologiaDocumental SCRUM-[ID]-Metadata.md Debe incluir: ticket autor fecha relación Prompt SCRUMCORE-167 relación legacy estado Documentar adicionalmente por regla DapperCrudEngine: método del DapperCrudEngine usado. QueryOptions construido. filtros aplicados. columnas explícitas. LockMode / InsertValues / UpdateValues usados. cómo se propaga el contexto transaccional. extensiones agregadas al DapperCrudEngine si fueron necesarias. evidencia de que no se usa Dapper directo ni SQL manual. ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ CRITERIOS DE ACEPTACIÓN ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Existe WorkflowStorageLogModel. Existe IWorkflowStorageLogRepository. Existe WorkflowStorageLogRepository. Existe IWorkflowStorageLogBuilder. Existe WorkflowStorageLogBuilder. Inserta solo si IdTareaWorkflow > 0. Usa transacción existente. No abre conexión. No commit. No rollback. SQL parametrizado. Compatible con campos legacy. Rollback revierte log. DI completo. Pruebas completas. Documentación completa. ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ RESTRICCIONES ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ NO filesystem. NO XML. NO AppResponses. NO Controller. NO SQL concatenado. NO commit. NO rollback. NO abrir conexión. NO exponer datos sensibles. NO usar Dapper directo desde repositories. NO usar IDbConnection.ExecuteAsync / QueryAsync / ExecuteScalarAsync desde repositories. NO omitir DapperCrudEngine. NO implementar SQL manual si puede modelarse con QueryOptions. NO usar SQL directo para FOR UPDATE; usar LockMode o extensión centralizada del DapperCrudEngine. NO usar SQL directo para INSERT/UPDATE; usar InsertValues/UpdateValues o extensión centralizada del DapperCrudEngine. ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ INSTRUCCIÓN FINAL ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Implementar WorkflowStorageLogRepository y WorkflowStorageLogBuilder para cubrir la funcionalidad legacy de logdocuarchi, integrados transaccionalmente con StorageTransactionCoordinator.

## Approach

- Convertir requerimientos del issue en deltas OpenSpec claros y testeables.
- Aplicar restricciones de repositorio, arquitectura y pruebas de OPSXJ_BACKEND_RULES.
- Definir alcance y no-alcance antes de implementar.
- Validar con openspec.cmd validate scrum-172-crea-funcion-workflow-storage-almacenami.