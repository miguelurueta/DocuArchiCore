## Context

- Jira issue key: SCRUM-110
- Jira summary: CREA-QUERYBUILDER-RESPOSITORY-BANDEJA-WORKFLOW
- Jira URL: https://contasoftcompany.atlassian.net/browse/SCRUM-110

## Context Reference

- openspec/context/multi-repo-context.md
- openspec/context/OPSXJ_BACKEND_RULES.md

## Problem Statement

PROMPT ARQUITECTÓNICO — QueryBuilder dinámico para bandeja workflow Rol esperado: Arquitecto de software senior y desarrollador backend .NET Nombre: WorkflowInboxQueryBuilder Proyecto destino: MiApp.Repository Ruta: /Repositorio/Workflow/BandejaCorrespondencia OBJETIVO Implementar el QueryBuilder dinámico de la bandeja workflow usando QueryOptions reales del proyecto y soportando los tres tipos de consulta heredados de la lógica legacy. ALCANCE FUNCIONAL Este componente: construye QueryOptions no ejecuta la consulta no arma UI no consulta metadata desde BD recibe metadata ya resuelta ENTRADA WorkflowInboxDynamicTableRequestDto request List<WorkflowDynamicColumnDefinitionDto> dynamicColumns RETORNO QueryOptions ARQUITECTURA APLICABLE Service / Builder interno ↓ Metadata resuelta ↓ QueryOptions CONTEXTO FUNCIONAL La tabla dinámica es: dat_adic_tar + nombreRuta Debe construirse como: TableName = $"dat_adic_tar{request.NombreRuta.Trim()} DAT" Debe incluir join con: estados_tarea_workflow etw ON etw.Inicio_Tareas_Workflow_id_Tarea = DAT.INICIO_TAREAS_WORKFLOW_ID_TAREA Debe respetar la lógica funcional de tres tipos de consulta: TIPO 1 solo filtros base TIPO 2 búsqueda LIKE dinámica sobre columnas text visibles y filtrables TIPO 3 expresión completa controlada sanitizada con whitelist de columnas válidas bloquear SQL peligroso REQUERIMIENTOS FUNCIONALES Crear interfaz: IWorkflowInboxQueryBuilder Crear clase: WorkflowInboxQueryBuilder Crear policy: WorkflowInboxQueryPolicy Método principal: QueryOptions Build(WorkflowInboxDynamicTableRequestDto request, List<WorkflowDynamicColumnDefinitionDto> dynamicColumns) TableName: $"dat_adic_tar{request.NombreRuta.Trim()} DAT" DefaultAlias: request.DefaultDbAlias.Trim() Columns del SELECT: Debe incluir siempre: etw.Inicio_Tareas_Workflow_id_Tarea AS id_tarea etw.Fecha_Inicio AS fecha_inicio DAT.ESTADO_TRAMITE AS ESTADO Más todas las columnas dinámicas visibles: $"{c.SqlColumnName} AS {c.Key}" Joins: "INNER JOIN estados_tarea_workflow etw ON etw.Inicio_Tareas_Workflow_id_Tarea = DAT.INICIO_TAREAS_WORKFLOW_ID_TAREA" Filtros base exactos: Id_Actividad = request.IdActividad Id_Usuario = request.IdUsuarioWorkflow Estado_Tarea = 0 ESTADO_ACTIVIDA_MODULO_RAD = 0 estado_modulo_radicado = 1 AdvancedFilters: Fecha_Seleccion IS NULL Fecha_Fin IS NULL Si EstadoTramite tiene valor y no es "Todos": agregar filter estado_tramite = request.EstadoTramite.Trim() Tipos de consulta TIPO 1 no agregar búsqueda adicional TIPO 2 usar request.Search solo columnas: IsFilterable = true DataType = text construir RawConditions con: AND (DAT.CAMPO1 LIKE '%valor%' OR DAT.CAMPO2 LIKE '%valor%' ...) escapar comillas simples del literal TIPO 3 usar request.Search como expresión avanzada sanitizar: permitir solo columnas presentes en dynamicColumns + fecha_inicio + ESTADO + id_tarea reemplazar: fecha_inicio → etw.Fecha_Inicio ESTADO → DAT.ESTADO_TRAMITE id_tarea → etw.Inicio_Tareas_Workflow_id_Tarea columna dinámica → DAT.columna si la expresión no es segura, no agregar RawConditions Orden seguro: whitelist con: todas las columnas dinámicas fecha_inicio ESTADO id_tarea SortField por defecto = fecha_inicio SortDir = ASC/DESC resolver columnas: fecha_inicio → etw.Fecha_Inicio ESTADO → DAT.ESTADO_TRAMITE id_tarea → etw.Inicio_Tareas_Workflow_id_Tarea dinámica → DAT.columna Paginación: Limit = request.PageSize <= 0 ? 25 : request.PageSize Offset = ((request.Page <= 0 ? 1 : request.Page) - 1) * Limit REQUERIMIENTOS TÉCNICOS Mantener métodos auxiliares: BuildColumns ApplySearch BuildOrder ResolveColumn EscapeSqlLiteral SanitizeExpression usar try/catch No usar SQL manual completo. Usar QueryOptions reales del proyecto. Joins debe ser List<string>, no inventar modelos de join no existentes. RawConditions debe ser List<string>. Mantener código listo para compilar con el DapperCrudEngine real. No mezclar lógica de repositorio ni service. ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ DOCUMENTACIÓN TÉCNICA ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Ubicación: /Docs/Workflow/BandejaCorrespondencia/WorkflowInboxQueryBuilder Archivos a crear o actualizar: SCRUM-[ID]-Diagramas.md Debe incluir: • Diagrama de casos de uso (si aplica) • Diagrama de clases (si aplica) • Diagrama de secuencia del nuevo flujo • Diagrama de estados (si aplica) Debe incluir: • Descripción técnica del contrato Documentar con comentarios XML: propósito de la clase propósito del método parámetros retorno comportamiento sin resultados comportamiento ante excepciones PRUEBAS UNITARIAS Incluir pruebas para: tipo 1 sin búsqueda tipo 2 con búsqueda válida tipo 2 sin columnas text tipo 3 con expresión válida tipo 3 con expresión peligrosa bloqueada order válido order inválido con fallback paginación correcta columnas dinámicas incluidas en SELECT filtros base aplicados REGISTRO EN Program.cs Registrar: IWorkflowInboxQueryBuilder WorkflowInboxQueryBuilder ENTREGABLES Interface Clase QueryBuilder Policy Comentarios XML Registro DI Pruebas unitarias Sin repository final Sin controller

## Approach

- Convertir requerimientos del issue en deltas OpenSpec claros y testeables.
- Aplicar restricciones de repositorio, arquitectura y pruebas de OPSXJ_BACKEND_RULES.
- Definir alcance y no-alcance antes de implementar.
- Validar con openspec.cmd validate scrum-110-crea-querybuilder-respository-bandeja-wo.