## Context

- Jira issue key: SCRUM-169
- Jira summary: CREA-FUNCION-EXPEDIENTE-UNIDAD-CONSERVACION-ALMACENAMINETO
- Jira URL: https://contasoftcompany.atlassian.net/browse/SCRUM-169

## Context Reference

- openspec/context/multi-repo-context.md
- openspec/context/OPSXJ_BACKEND_RULES.md

## Problem Statement

PROMPT ARQUITECTÓNICO — ORCHESTRATOR / ENGINE PROMPT 8 — Fase archivística transaccional: Expediente + Unidad de Conservación + Índice Electrónico (ENTERPRISE CRÍTICO — SRP / SOLID, COMPLETO Y DOCUMENTADO) ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ ROL ESPERADO ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Arquitecto Master Backend .NET especialista en: Clean Architecture SRP (Single Responsibility Principle) sistemas ECM / TRD expedientes electrónicos unidades de conservación índices electrónicos documentales concurrencia con SELECT FOR UPDATE DapperCrudEngine + QueryOptions transaccional observabilidad avanzada pruebas unitarias, integración, concurrencia y QT documentación enterprise ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ OBJETIVO ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Implementar la fase archivística del Storage Engine: ✔ Validar expediente ✔ Validar unidad de conservación ✔ Validar estado archivístico (físico y electrónico) ✔ Actualizar folios expediente ✔ Actualizar folios unidad ✔ Calcular índice electrónico ✔ Construir modelo de índice ✔ Insertar índice electrónico ✔ Garantizar consistencia dentro de la misma transacción ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ REGLA ARQUITECTÓNICA CRÍTICA (SRP) ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Implementación obligatoriamente separada en: IExpedienteRepository / ExpedienteRepository IUnidadConservacionRepository / UnidadConservacionRepository IIndiceElectronicoRepository / IndiceElectronicoRepository IIndiceElectronicoCalculator / IndiceElectronicoCalculator IIndiceElectronicoBuilder / IndiceElectronicoBuilder StorageTransactionCoordinator (orquestación) PROHIBIDO: Clase monolítica Lógica mezclada entre repositorio/cálculo/orquestación ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ CONTEXTO Y DEPENDENCIAS ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Depende de: PROMPT 2 SCRUMCORE-163 → Contratos PROMPT 6 SCRUMCORE-166→ TransactionCoordinator PROMPT 7 SCRUMCORE-167→ Persistencia DB Flujo: TransactionCoordinator → ExpedienteRepository (lock) → UnidadRepository (lock) → Calculator → Update folios → Builder → IndiceRepository → Commit / Rollback REFERENCIA DE TABLAS Ver: Docs/DataModel/StorageEngine-Tables.md Tablas usadas en este prompt: expediente_archivo unidad_conservacion -ra_cert_indice_expediente ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ REGLA GLOBAL OBLIGATORIA — ACCESO A DATOS CON DAPPERCRUDENGINE ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Toda función de repositorio que realice consulta, inserción, actualización, eliminación, bloqueo lógico o lectura transaccional de datos DEBE usar obligatoriamente: DapperCrudEngine QueryOptions QueryFilter / filtros equivalentes del motor modelos tipados de entrada/salida contexto transaccional soportado por el DapperCrudEngine cuando aplique Ruta de referencia obligatoria: D:\imagenesda\GestorDocumental\DocuArchiCore\MiApp.Repository\Repositorio\DataAccess\DapperCrudEngine.cs Regla de arquitectura: Repository = DapperCrudEngine + QueryOptions PROHIBIDO en repositories: connection.ExecuteAsync(...) connection.QueryAsync(...) connection.ExecuteScalarAsync(...) SQL manual concatenado SQL interpolado con datos de usuario construir comandos SQL libres fuera del DapperCrudEngine abrir conexiones directamente abrir transacciones directamente hacer commit o rollback retornar AppResponses<T> resolver claims o lógica HTTP Permitido únicamente si el DapperCrudEngine ya lo soporta o se implementa primero como extensión centralizada: ejecución transaccional mediante DapperCrudEngine QueryOptions con TransactionContext QueryOptions con LockMode = ForUpdate QueryOptions con Insert/Update/Delete tipado QueryOptions con condición de concurrencia QueryOptions con retorno de identidad generada Si el DapperCrudEngine actual NO soporta alguna operación requerida por este prompt, se debe crear primero una extensión centralizada del DapperCrudEngine, documentada y probada, antes de implementar el repository. Queda prohibido saltarse el motor con Dapper directo. ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ REGLA TRANSACCIONAL CON DAPPERCRUDENGINE ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Cuando una operación de repository deba participar en una transacción del Storage Engine: El TransactionCoordinator abre la conexión/transacción o solicita al DapperCrudEngine un contexto transaccional. El repository NO manipula IDbConnection/IDbTransaction directamente salvo que el DapperCrudEngine exponga un contexto transaccional tipado. El repository recibe un contexto de ejecución transaccional, por ejemplo: StorageDbExecutionContext DapperCrudTransactionContext QueryExecutionContext Usar el nombre real disponible en el proyecto. Toda consulta, insert, update, lock o condición de concurrencia debe ejecutarse a través de DapperCrudEngine. Si se requiere SELECT ... FOR UPDATE, debe modelarse mediante QueryOptions.LockMode = ForUpdate o una propiedad equivalente. Si se requiere UPDATE con condición de concurrencia, debe modelarse en QueryOptions con filtros WHERE adicionales. Si se requiere INSERT y retorno de identidad, debe usarse método centralizado del DapperCrudEngine. Si no existe, implementarlo en el motor. ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ UBICACIÓN ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Repositories: MiApp.Repository/Repositorio/GestorDocumental/AlmacenamientoDocumental/Expediente/ Services: MiApp.Services/Service/GestorDocumental/AlmacenamientoDocumental/Expediente/ Docs: Docs/GestorDocumental/AlmacenamientoDocumental/StorageEngine/ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ MODELOS ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ (SIN CAMBIO — usar los definidos previamente) ExpedienteInfoModel UnidadConservacionInfoModel IndiceElectronicoInsertModel IndiceElectronicoCalculationResult ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ AJUSTE ESPECÍFICO PROMPT 8 — EXPEDIENTE / UNIDAD / ÍNDICE CON DAPPERCRUDENGINE ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Todos los repositories de esta fase deben usar DapperCrudEngine + QueryOptions. ExpedienteRepository.LockExpedienteAsync: QueryOptions.TableName = tabla real de expediente. Columns explícitas. Filters por IdExpediente. Limit = 1. LockMode = ForUpdate. UnidadConservacionRepository.LockAsync: QueryOptions.TableName = tabla real de unidad. Columns explícitas. Filters por IdUnidad. Limit = 1. LockMode = ForUpdate. UpdateFoliosAsync / UpdateIndiceAsync: QueryOptions con UpdateValues. Filters por ID. Validar rows == 1. IndiceElectronicoRepository.InsertAsync: QueryOptions con InsertValues. No SQL manual. Validar rows == 1. Si QueryOptions no soporta LockMode, UpdateValues o InsertValues, se debe extender primero el DapperCrudEngine. No se permite usar SQL directo como atajo. ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ VALIDACIONES CRÍTICAS ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ expediente != null unidad != null EstadoExpediente == 1 EstadoExpedienteElectronico == 1 numeroFolios > 0 IdRegistroProduccion > 0 NombreDocumento requerido TipoDocumento requerido RutaDocumento requerido Formato requerido Tamano requerido Si falla → throw StorageTransactionException ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ CÁLCULO DEL ÍNDICE ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Responsabilidad: IIndiceElectronicoCalculator Reglas: nuevoOrden = expediente.OrdenIndice + 1 paginaInicial = expediente.UltimaPaginaIndice + 1 paginaFinal = paginaInicial + numeroFolios - 1 Validar: paginaFinal >= paginaInicial no overflow de folios no salto de páginas no colisión de orden ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ HASH DOCUMENTAL ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Responsabilidad: IIndiceElectronicoBuilder Reglas: SHA256 obligatorio determinístico reproducible NO MD5 NO dependiente de valores volátiles ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ REGLAS TRANSACCIONALES ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ SELECT FOR UPDATE obligatorio todo dentro de la misma transacción no commit en repositorios no rollback en repositorios orden de ejecución fijo: expediente → unidad → índice ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ OBSERVABILIDAD (DETALLADA) ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Logs obligatorios: INFO: expediente lock OK unidad lock OK cálculo índice OK update folios OK insert índice OK WARNING: hash lógico usado datos opcionales faltantes ERROR: expediente inexistente unidad inexistente expediente cerrado error update error insert índice Campos obligatorios: requestId alias usuarioId idExpediente idUnidad idAlmacen idRegistroProduccion nuevoOrden paginaInicial paginaFinal numeroFolios fase = ExpedienteIndex duraciónMs Métricas: storage_expediente_lock_success storage_expediente_lock_error storage_indice_insert_success storage_indice_insert_error storage_concurrency_conflict ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ PRUEBAS (DETALLADAS) ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ UNITARIAS Calculator: cálculo correcto overflow valores inválidos Builder: mapping correcto hash SHA256 válido Repositories: lock expediente lock unidad update correcto insert índice correcto INTEGRACIÓN SELECT FOR UPDATE real update expediente real update unidad real insert índice real commit OK rollback OK CONCURRENCIA 2 threads → no colisión orden 2 threads → no colisión páginas bloqueo efectivo QT / CALIDAD no SELECT * no SQL libre logs correctos sin datos sensibles sin fuga de transacción Pruebas adicionales obligatorias por regla DapperCrudEngine: verificar que el repository invoca DapperCrudEngine y no IDbConnection directo. verificar que se construye QueryOptions con TableName correcto. verificar que se usan filtros parametrizados. verificar que LockMode = ForUpdate se usa cuando aplique. verificar que InsertValues/UpdateValues se usan cuando aplique. verificar que no existe ExecuteAsync/QueryAsync/ExecuteScalarAsync directo en el repository. verificar que no existe SQL concatenado. verificar que el contexto transaccional se propaga al DapperCrudEngine. ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ DOCUMENTACIÓN ENTERPRISE (OBLIGATORIA) ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Ruta: Docs/GestorDocumental/AlmacenamientoDocumental/StorageEngine/ 1. SCRUM-[ID]-Arquitectura-Expediente-Indice.md Debe incluir: objetivo arquitectónico SRP aplicado diagrama de clases diagrama de secuencia diagrama de estados: locked updated indexed rollback flujo completo relación con prompts SCRUMCORE-166 y SCRUMCORE-167 decisiones de diseño riesgos (deadlocks, concurrencia) 2. SCRUM-[ID]-Implementacion-Detallada-Expediente-Indice.md Debe incluir: archivos creados rutas exactas responsabilidades por clase lógica de cálculo lógica de builder SQL conceptual validaciones decisiones técnicas 3. SCRUM-[ID]-Pruebas-Expediente-Indice.md Debe incluir: matriz de pruebas unitarias integración concurrencia QT resultados esperados 4. SCRUM-[ID]-Observabilidad-Expediente-Indice.md Debe incluir: logs definidos métricas ejemplos troubleshooting: deadlock expediente cerrado colisión orden error índice 5. SCRUM-[ID]-Concurrencia-Expediente-Indice.md Debe incluir: estrategia locking FOR UPDATE riesgos deadlock mitigaciones 6. SCRUM-[ID]-Seguridad-Hash.md Debe incluir: SHA256 razones riesgos futuro hash físico 7. SCRUM-[ID]-Metadata.md Debe incluir: ticket autor fecha dependencias (Prompt SCRUMCORE-166,SCRUMCORE-167) Documentar adicionalmente por regla DapperCrudEngine: método del DapperCrudEngine usado. QueryOptions construido. filtros aplicados. columnas explícitas. LockMode / InsertValues / UpdateValues usados. cómo se propaga el contexto transaccional. extensiones agregadas al DapperCrudEngine si fueron necesarias. evidencia de que no se usa Dapper directo ni SQL manual. ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ ENTREGABLES ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Código: repositories calculator builder integración coordinator DI pruebas unitarias pruebas integración pruebas concurrencia pruebas QT Documentación: Arquitectura Implementación Pruebas Observabilidad Concurrencia Seguridad Metadata ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ CRITERIOS DE ACEPTACIÓN ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ ✔ SRP respetado ✔ Calculator separado ✔ Builder separado ✔ Repository separado ✔ Coordinator sin lógica ✔ No colisiones ✔ No inconsistencias ✔ Transacción correcta ✔ Logs completos ✔ Pruebas completas ✔ Documentación completa ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ RESTRICCIONES ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ NO filesystem NO XML NO compensation NO API NO commit en repos NO rollback en repos NO SQL libre NO SELECT * NO MD5 NO usar Dapper directo desde repositories. NO usar IDbConnection.ExecuteAsync / QueryAsync / ExecuteScalarAsync desde repositories. NO omitir DapperCrudEngine. NO implementar SQL manual si puede modelarse con QueryOptions. NO usar SQL directo para FOR UPDATE; usar LockMode o extensión centralizada del DapperCrudEngine. NO usar SQL directo para INSERT/UPDATE; usar InsertValues/UpdateValues o extensión centralizada del DapperCrudEngine. ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ INSTRUCCIÓN FINAL ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Implementar la fase archivística completa respetando: SRP Clean Architecture consistencia transaccional seguridad trazabilidad observabilidad

## Approach

- Convertir requerimientos del issue en deltas OpenSpec claros y testeables.
- Aplicar restricciones de repositorio, arquitectura y pruebas de OPSXJ_BACKEND_RULES.
- Definir alcance y no-alcance antes de implementar.
- Validar con openspec.cmd validate scrum-169-crea-funcion-expediente-unidad-conservac.