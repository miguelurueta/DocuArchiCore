## Context

- Jira issue key: SCRUM-167
- Jira summary: CREA-FUNCION-COORDINADOR-TRANSACCIONES-ALMACENAMIENTO
- Jira URL: https://contasoftcompany.atlassian.net/browse/SCRUM-167

## Context Reference

- openspec/context/multi-repo-context.md
- openspec/context/OPSXJ_BACKEND_RULES.md

## Problem Statement

PROMPT ARQUITECTÓNICO — ORCHESTRATOR / ENGINE PROMPT 6 — StorageTransactionCoordinator extendido (ENTERPRISE CRÍTICO — SYSTEM1 + DISCO_DETALLE + PREPARADO PARA WORKFLOW LOG) ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ ROL ESPERADO ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Actúa como Arquitecto Master Backend .NET, especialista en: Clean Architecture Unit of Work manual transacciones críticas MySQL DapperCrudEngine QueryOptions SELECT FOR UPDATE coordinación de repositorios transaccionales consistencia fuerte rollback controlado concurrencia cobertura funcional legacy observabilidad transaccional ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ OBJETIVO ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Extender StorageTransactionCoordinator para coordinar en una única transacción DB: reserva de identidad con system1 validación de capacidad disco_detalle inserción futura de gabinete e inventario actualización final de disco_detalle preparación para log workflow commit/rollback centralizado Este prompt sigue sin implementar filesystem/XML/compensation física. ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ CONTEXTO LEGACY CUBIERTO ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ El legacy realiza en flujo transaccional: lock system1 update system1 insert gabinete insert inventario update expediente/unidad insert índice insert logdocuarchi si id_tarea_workflow <> 0 update disco_detalle NUMERO_IMAGENES / NUMPAG_CARP commit final Este prompt debe dejar el coordinator preparado para ese orden, sin asumir filesystem dentro de DB. REFERENCIA DE TABLAS Ver: Docs/DataModel/StorageEngine-Tables.md Tablas usadas en este prompt: disco_detalle ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ REGLA GLOBAL OBLIGATORIA — ACCESO A DATOS CON DAPPERCRUDENGINE ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Toda función de repositorio que realice consulta, inserción, actualización, eliminación, bloqueo lógico o lectura transaccional de datos DEBE usar obligatoriamente: DapperCrudEngine QueryOptions QueryFilter / filtros equivalentes del motor modelos tipados de entrada/salida contexto transaccional soportado por el DapperCrudEngine cuando aplique Ruta de referencia obligatoria: D:\imagenesda\GestorDocumental\DocuArchiCore\MiApp.Repository\Repositorio\DataAccess\DapperCrudEngine.cs Regla de arquitectura: Repository = DapperCrudEngine + QueryOptions PROHIBIDO en repositories: connection.ExecuteAsync(...) connection.QueryAsync(...) connection.ExecuteScalarAsync(...) SQL manual concatenado SQL interpolado con datos de usuario construir comandos SQL libres fuera del DapperCrudEngine abrir conexiones directamente abrir transacciones directamente hacer commit o rollback retornar AppResponses<T> resolver claims o lógica HTTP Permitido únicamente si el DapperCrudEngine ya lo soporta o se implementa primero como extensión centralizada: ejecución transaccional mediante DapperCrudEngine QueryOptions con TransactionContext QueryOptions con LockMode = ForUpdate QueryOptions con Insert/Update/Delete tipado QueryOptions con condición de concurrencia QueryOptions con retorno de identidad generada Si el DapperCrudEngine actual NO soporta alguna operación requerida por este prompt, se debe crear primero una extensión centralizada del DapperCrudEngine, documentada y probada, antes de implementar el repository. Queda prohibido saltarse el motor con Dapper directo. ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ REGLA TRANSACCIONAL CON DAPPERCRUDENGINE ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Cuando una operación de repository deba participar en una transacción del Storage Engine: El TransactionCoordinator abre la conexión/transacción o solicita al DapperCrudEngine un contexto transaccional. El repository NO manipula IDbConnection/IDbTransaction directamente salvo que el DapperCrudEngine exponga un contexto transaccional tipado. El repository recibe un contexto de ejecución transaccional, por ejemplo: StorageDbExecutionContext DapperCrudTransactionContext QueryExecutionContext Usar el nombre real disponible en el proyecto. Toda consulta, insert, update, lock o condición de concurrencia debe ejecutarse a través de DapperCrudEngine. Si se requiere SELECT ... FOR UPDATE, debe modelarse mediante QueryOptions.LockMode = ForUpdate o una propiedad equivalente. Si se requiere UPDATE con condición de concurrencia, debe modelarse en QueryOptions con filtros WHERE adicionales. Si se requiere INSERT y retorno de identidad, debe usarse método centralizado del DapperCrudEngine. Si no existe, implementarlo en el motor. ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ DEPENDENCIAS ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Depende de: Prompt 2 SCRUMCORE-163 Prompt 4 SCRUMCORE-165 Prompt 5 SCRUMCORE-166 Prompt 7 SCRUMCORE-168 Prompt 8 SCRUMCORE-169 Nuevo Prompt WorkflowStorageLogRepository ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ UBICACIÓN ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ MiApp.Services/Service/GestorDocumental/AlmacenamientoDocumental/Transaction/ Archivos: IStorageTransactionCoordinator.cs StorageTransactionCoordinator.cs StorageTransactionResult.cs Documentación: Docs/GestorDocumental/AlmacenamientoDocumental/StorageEngine/ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ MODELO — StorageTransactionResult ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ public sealed class StorageTransactionResult { public required StorageIdentityReservationResult IdentityReservation { get; init; } public long? IdRegistroProduccionDocumental { get; init; }

public bool Success { get; init; }

public StorageDocumentState Estado { get; init; }

public string RequestId { get; init; } = string.Empty;

public DateTime FechaEjecucion { get; init; }

public long DuracionMs { get; init; }

public bool DiskUsageUpdated { get; init; }

public bool WorkflowLogInserted { get; init; } } ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ DEPENDENCIAS INYECTADAS ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ StorageTransactionCoordinator debe inyectar: IDbConnectionFactory IStorageIdentityAllocator IStorageDiskQuotaRepository ILogger<StorageTransactionCoordinator> Cuando existan prompts posteriores: IGabineteStorageRepository IInventarioDocumentalRepository IExpedienteRepository IUnidadConservacionRepository IIndiceElectronicoRepository IWorkflowStorageLogRepository ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ FLUJO TRANSACCIONAL OBJETIVO ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Dentro de una sola conexión y una sola transacción: Validar context. Abrir conexión. BeginTransaction(IsolationLevel.Serializable). Ejecutar IdentityAllocator: lock system1 lock disco_detalle update system1 Insert gabinete dinámico si Prompt 7 ya está implementado. Insert inventario si aplica. Ejecutar fase expediente/unidad/índice si aplica. Insertar log workflow si: context.Command.Workflow != null IdTareaWorkflow > 0 Actualizar disco_detalle: NUMERO_IMAGENES NUMPAG_CARP Commit. Retornar StorageTransactionResult. Si cualquier paso falla: rollback total. lanzar StorageTransactionException. no filesystem. no XML. ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ ACTUALIZACIÓN DISCO_DETALLE ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Responsabilidad: El lock y validación de disco_detalle se hacen en Prompt 5. El update final de disco_detalle se coordina aquí antes del commit. Regla: NuevoNumeroImagenes = status.NumeroImagenes + context.Command.NumeroPaginasDeclaradas NuevoNumPagCarp = reservation.NewFolderPages Usar: IStorageDiskQuotaRepository.UpdateDiskUsageAsync(...) Si rows != 1 → StorageTransactionException. ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ INTEGRACIÓN WORKFLOW LOG ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ No implementar aquí el SQL del log. Solo preparar punto de extensión: if (context.Command.Workflow?.IdTareaWorkflow > 0) { await _workflowStorageLogRepository.InsertAsync(...); } La implementación real queda en: PROMPT SCRUMCORE-172 — WorkflowStorageLogRepository ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ REGLAS TRANSACCIONALES OBLIGATORIAS ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Una sola conexión. Una sola transacción. IsolationLevel.Serializable. No transacciones anidadas. No commit parcial. Commit únicamente al final. Rollback ante cualquier excepción. IdentityAllocator exactamente una vez. Update disco_detalle antes del commit. Workflow log dentro de la misma transacción. No filesystem. No XML. No compensation física. ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ ORDEN DE BLOQUEOS ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Orden recomendado para evitar deadlocks: system1 disco_detalle gabinete/inventario expediente unidad índice workflow log Documentar cualquier cambio. ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ AJUSTE ESPECÍFICO PROMPT 6 — TRANSACTIONCOORDINATOR Y DAPPERCRUDENGINE ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ El TransactionCoordinator debe coordinar la transacción sin forzar a los repositories a usar Dapper directo. Regla: El TransactionCoordinator puede crear o solicitar un contexto transaccional al DapperCrudEngine. Los repositories reciben ese contexto transaccional. Los repositories ejecutan todo por DapperCrudEngine. Ejemplo conceptual: using var txContext = await _DaperCrudEngine.BeginTransactionAsync( context.DefaultDbAlias, IsolationLevel.Serializable ); try { var reservation = await _identityAllocator.ReserveAsync(context, txContext); await _diskQuotaRepository.UpdateDiskUsageAsync(model, txContext);

await txContext.CommitAsync(); } catch { await txContext.RollbackAsync(); throw; } Si el DapperCrudEngine actual no expone BeginTransactionAsync o TransactionContext, se debe implementar una extensión centralizada del DapperCrudEngine antes de continuar. No se permite volver a connection.ExecuteAsync dentro de repositories. ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ IMPLEMENTACIÓN — StorageTransactionCoordinator ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Mantener estructura existente: validaciones iniciales Stopwatch logging GetOpenConnectionAsync validar ConnectionState.Open BeginTransaction(IsolationLevel.Serializable) try/catch rollback protegido StorageTransactionException Extender después de reservation: var reservation = await _identityAllocator.ReserveAsync(context, connection, transaction); // Fases posteriores DB await _diskQuotaRepository.UpdateDiskUsageAsync( new DiskQuotaUpdateModel { Disco = reservation.Identity.Disco, NombreGabinete = context.Command.NombreGabinete, NuevoNumeroImagenes = calculatedValue, NuevoNumPagCarp = reservation.NewFolderPages }, connection, transaction ); transaction.Commit(); ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ OBSERVABILIDAD ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Logs obligatorios adicionales: INFO: disk quota update iniciado disk quota update OK workflow log omitido/no aplica workflow log insertado si aplica WARNING: workflow informado incompleto disco_detalle no actualizado por error controlado ERROR: update disco_detalle fallido workflow log fallido rollback por fallo posterior a identidad Campos: requestId alias usuarioId nombreGabinete disco idAlmacen idRegistroProduccion diskUsageUpdated workflowLogInserted estado duraciónMs ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ PRUEBAS OBLIGATORIAS ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Unitarias: IdentityAllocator se ejecuta una vez DiskQuota update se ejecuta después de identidad DiskQuota update falla → rollback Workflow null → no intenta log Workflow.IdTareaWorkflow = 0 → no intenta log Workflow.IdTareaWorkflow > 0 → intenta log log workflow falla → rollback éxito retorna DiskUsageUpdated = true éxito retorna WorkflowLogInserted según caso Integración: commit confirma update system1 + disco_detalle rollback revierte update system1 + disco_detalle error en disk update revierte todo workflow log dentro de misma transacción concurrencia sin duplicar identidad QT: validar orden de locks validar logs sin datos sensibles validar no filesystem/XML validar no commit parcial Pruebas adicionales obligatorias por regla DapperCrudEngine: verificar que el repository invoca DapperCrudEngine y no IDbConnection directo. verificar que se construye QueryOptions con TableName correcto. verificar que se usan filtros parametrizados. verificar que LockMode = ForUpdate se usa cuando aplique. verificar que InsertValues/UpdateValues se usan cuando aplique. verificar que no existe ExecuteAsync/QueryAsync/ExecuteScalarAsync directo en el repository. verificar que no existe SQL concatenado. verificar que el contexto transaccional se propaga al DapperCrudEngine. ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ DOCUMENTACIÓN OBLIGATORIA ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Crear/actualizar: SCRUM-[ID]-Arquitectura-TransactionCoordinator.md Agregar: flujo extendido con disco_detalle punto de extensión workflow log orden de locks relación con legacy relación con Prompt 11 SCRUM-[ID]-Implementacion-Detallada-TransactionCoordinator.md Agregar: DiskQuotaRepository inyectado actualización disco_detalle flags del StorageTransactionResult punto de extensión workflow SCRUM-[ID]-Pruebas-TransactionCoordinator.md Agregar: pruebas disco_detalle pruebas workflow log rollback por fallo disk/log SCRUM-[ID]-Observabilidad-TransactionCoordinator.md Agregar: logs disk usage logs workflow troubleshooting disco_detalle SCRUM-[ID]-Cobertura-Legacy-TransactionCoordinator.md Debe incluir: update disco_detalle legacy → DiskQuotaRepository logdocuarchi legacy → Prompt 11 commit legacy → TransactionCoordinator rollback legacy → TransactionCoordinator SCRUM-[ID]-Metadata.md Actualizar dependencias: Prompt 5 extendido Prompt 7 Prompt 8 Prompt 11 workflow log Documentar adicionalmente por regla DapperCrudEngine: método del DapperCrudEngine usado. QueryOptions construido. filtros aplicados. columnas explícitas. LockMode / InsertValues / UpdateValues usados. cómo se propaga el contexto transaccional. extensiones agregadas al DapperCrudEngine si fueron necesarias. evidencia de que no se usa Dapper directo ni SQL manual. ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ CRITERIOS DE ACEPTACIÓN ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Mantiene funcionalidad existente del Prompt 6. Integra actualización disco_detalle. Prepara integración WorkflowStorageLogRepository. Rollback revierte identidad + disco_detalle. Commit solo al final. StorageTransactionResult informa DiskUsageUpdated. StorageTransactionResult informa WorkflowLogInserted. No filesystem. No XML. Pruebas completas. Documentación actualizada. Listo para Prompt 11. ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ RESTRICCIONES ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ NO filesystem. NO XML. NO compensation física. NO SQL fuera de repositories. NO lógica de log workflow dentro del coordinator. NO commit parcial. NO retry automático. NO transacciones anidadas. NO usar Dapper directo desde repositories. NO usar IDbConnection.ExecuteAsync / QueryAsync / ExecuteScalarAsync desde repositories. NO omitir DapperCrudEngine. NO implementar SQL manual si puede modelarse con QueryOptions. NO usar SQL directo para FOR UPDATE; usar LockMode o extensión centralizada del DapperCrudEngine. NO usar SQL directo para INSERT/UPDATE; usar InsertValues/UpdateValues o extensión centralizada del DapperCrudEngine. ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ INSTRUCCIÓN FINAL ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Extender StorageTransactionCoordinator para cubrir disco_detalle y dejar punto formal de integración para WorkflowStorageLogRepository, manteniendo transacción única, rollback total, observabilidad y documentación enterprise.

## Approach

- Convertir requerimientos del issue en deltas OpenSpec claros y testeables.
- Aplicar restricciones de repositorio, arquitectura y pruebas de OPSXJ_BACKEND_RULES.
- Definir alcance y no-alcance antes de implementar.
- Validar con openspec.cmd validate scrum-167-crea-funcion-coordinador-transacciones-a.