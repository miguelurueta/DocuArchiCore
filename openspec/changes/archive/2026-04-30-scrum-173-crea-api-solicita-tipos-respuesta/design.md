## Context

- Jira issue key: SCRUM-173
- Jira summary: CREA-API-SOLICITA-TIPOS-RESPUESTA
- Jira URL: https://contasoftcompany.atlassian.net/browse/SCRUM-173

## Context Reference

- openspec/context/multi-repo-context.md
- openspec/context/OPSXJ_BACKEND_RULES.md

## Problem Statement

PROMPT ARQUITECTÓNICO — Ticket BE Crear API + Service + Repository: SolicitaListaTiposRespuesta Catálogo: ra_tipo_respuesta (ENTERPRISE FINAL — DapperCrudEngine + QueryOptions + Cache + Documentación Completa) ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ ROL ESPERADO ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Actúa como Arquitecto de Software Senior Backend .NET, especialista en: Core Clean Architecture Controller → Service → Repository DapperCrudEngine QueryOptions AppResponses<T> AppMetaBuilder AppError seguridad por claims catálogos reutilizables caching distribuido resiliencia observabilidad pruebas unitarias, integración, QT y regresión documentación técnica enterprise ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ OBJETIVO ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Implementar una API backend para consultar la lista de tipos de respuesta disponibles para selección en frontend. Fuente de datos: Tabla: ra_tipo_respuesta Reglas funcionales: Retornar únicamente registros activos: estado = 1 Ordenar por: orden ASC Exponer solo: ID Descripcion Retornar DTO liviano reutilizable para combos/listas Usar contrato estándar AppResponses<List<ResponseDrowp>> La solución debe cumplir: arquitectura desacoplada seguridad por claim defaulalias uso obligatorio de DapperCrudEngine uso obligatorio de QueryOptions prohibición de SQL manual directo repository sin AppResponses service como responsable de AppResponses/cache/errores controller sin lógica de negocio cache enterprise distribuido pruebas completas documentación técnica completa ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ CONTEXTO EXISTENTE ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Tabla existente: ra_tipo_respuesta Campos relevantes: id_tipo_respuesta codigo descripcion_tipo_respuesta estado orden created_at updated_at created_by Consulta funcional legacy/base: SELECT id_tipo_respuesta AS ID, descripcion_tipo_respuesta AS Descripcion FROM ra_tipo_respuesta WHERE estado = 1 ORDER BY orden ASC; ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ REGLA GLOBAL OBLIGATORIA DE ACCESO A DATOS ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Toda función de repositorio que realice consulta, inserción, actualización o eliminación de datos debe usar obligatoriamente: DapperCrudEngine QueryOptions Ruta de referencia obligatoria: D:\imagenesda\GestorDocumental\DocuArchiCore\MiApp.Repository\Repositorio\DataAccess\DapperCrudEngine.cs PROHIBIDO en repositories: SQL manual concatenado ExecuteAsync directo sobre IDbConnection QueryAsync directo sobre IDbConnection ExecuteScalarAsync directo sobre IDbConnection interpolación SQL con valores de usuario construir SQL libre fuera de QueryOptions retornar AppResponses desde repository Permitido: QueryOptions QueryFilter _DaperCrudEngine.GetAllAsync<T>(options) métodos centralizados del DapperCrudEngine disponibles en el proyecto ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ UBICACIÓN ESPERADA ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Controller: DocuArchi.Api/Controllers/GestionCorrespondencia/TiposRespuesta/SolicitaListaTiposRespuestaController.cs Service: MiApp.Services/Service/GestionCorrespondencia/TiposRespuesta/ServiceSolicitaListaTiposRespuesta.cs Repository: MiApp.Repository/Repositorio/GestionCorrespondencia/TiposRespuesta/SolicitaListaTiposRespuestaRepository.cs DTO reutilizado: MiApp.DTOs/DTOs/Common/ResponseDrowp.cs Documentación: Docs/GestionCorrespondencia/TiposRespuesta/ Pruebas: tests/DocuArchi.Api.Tests/GestionCorrespondencia/TiposRespuesta/ tests/MiApp.Services.Tests/GestionCorrespondencia/TiposRespuesta/ tests/MiApp.Repository.Tests/GestionCorrespondencia/TiposRespuesta/ tests/MiApp.IntegrationTests/GestionCorrespondencia/TiposRespuesta/ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ RUTA OFICIAL ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ GET /api/gestion-correspondencia/tipos-respuesta Ruta opcional futura documentada, NO implementar salvo estándar del proyecto: GET /api/catalogos/tipos-respuesta ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ CONTRATO OFICIAL ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Request: Sin parámetros de query Alias NO viene del request Alias viene exclusivamente del claim defaulalias Response: AppResponses<List<ResponseDrowp>> ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ DTO OFICIAL ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Usar DTO existente: public class ResponseDrowp { public long ID { get; set; } public string Descripcion { get; set; } = string.Empty; } Reglas: NO crear DTO nuevo para este ticket. Mantener compatibilidad con frontend actual. Documentar deuda técnica de naming: ResponseDrowp parece tener error ortográfico. Futuro recomendado: ResponseDropdownDto. No renombrar en este ticket para evitar breaking changes. ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ REGLAS DE NEGOCIO ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Consultar tabla ra_tipo_respuesta. Filtrar estado = 1. Ordenar por orden ASC. Retornar máximo 500 registros como protección defensiva. Retornar solo: ID Descripcion No exponer: codigo estado orden created_at updated_at created_by Si no hay registros: success = true data = lista vacía meta.Status = "empty" ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ SEGURIDAD ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Claim obligatorio: defaulalias Reglas: defaulalias debe validarse usando IClaimValidationService. Si claim inválido o ausente: retornar BadRequest(validation.Response) El alias NO debe recibirse por querystring. El alias NO debe recibirse por body. El alias NO debe hardcodearse. No exponer datos sensibles. No permitir override de alias desde frontend. ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ CONTROLLER ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Crear: SolicitaListaTiposRespuestaController.cs Responsabilidad: Exponer endpoint HTTP. Validar claim defaulalias. Generar o propagar requestId si el estándar del proyecto lo permite. Invocar service. Retornar Ok(result) si result.Success = true. Retornar BadRequest(result) si result.Success = false. No contener lógica de negocio. No acceder a repository. No acceder a cache. No construir QueryOptions. Dependencias: IClaimValidationService IServiceSolicitaListaTiposRespuesta ILogger<SolicitaListaTiposRespuestaController> Flujo: Recibir GET. Validar defaulalias. Si inválido, retornar BadRequest(validation.Response). Registrar log de inicio con requestId y alias. Llamar service: SolicitaListaTiposRespuestaAsync(defaultDbAlias, requestId) Si Success = true: Ok(result) Si Success = false: BadRequest(result) Capturar error inesperado solo como última barrera si el patrón del proyecto lo exige. ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ SERVICE ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Crear: IServiceSolicitaListaTiposRespuesta ServiceSolicitaListaTiposRespuesta Responsabilidad: Validar alias. Aplicar estrategia cache-first. Invocar repository si cache miss o cache falla. Construir AppResponses<List<ResponseDrowp>>. Construir metadata con AppMetaBuilder. Manejar errores controlados. Registrar logs de negocio. No construir SQL. No acceder directamente a DapperCrudEngine. Dependencias: ISolicitaListaTiposRespuestaRepository IDistributedCache o ICacheService equivalente del proyecto ILogger<ServiceSolicitaListaTiposRespuesta> configuración de cache si aplica Método sugerido: Task<AppResponses<List<ResponseDrowp>>> SolicitaListaTiposRespuestaAsync( string defaultDbAlias, string requestId ); Reglas: try/catch obligatorio. defaultDbAlias requerido. Cache por alias. Si cache falla, no debe fallar el endpoint. Si DB falla, retornar AppResponses con Success = false. Repository retorna List<ResponseDrowp>, nunca AppResponses. Service decide semántica SUCCESS / EMPTY / ERROR. ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ CACHE ENTERPRISE ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Tipo: Redis obligatorio en entornos distribuidos. MemoryCache permitido solo en mono-instancia o ambiente local. Clave: catalogo:ra_tipo_respuesta:{alias} TTL: configurable. recomendado inicial: 10 minutos. Reglas: cache por alias. serialización JSON. fallback a DB si cache falla. cache miss consulta DB. cache hit retorna sin DB. permitir bypass por configuración: CatalogosCache:Enabled = true/false implementar protección contra cache stampede: lock por clave si existe mecanismo del proyecto si no existe, documentar deuda técnica controlada invalidación mediante una de estas estrategias: endpoint administrativo futuro evento de dominio futuro versionado de clave TTL corto NO hacer: cache global sin alias. cache sin TTL. cache de errores. fallar endpoint por error de Redis. ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ SEMÁNTICA DE RESPUESTA ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ SUCCESS con datos: Success = true Message = "YES" Data = lista Meta.Status = "success" EMPTY: Success = true Message = "Sin resultados" Data = lista vacía Meta.Status = "empty" ERROR: Success = false Message = "Error consultando tipos de respuesta" Data = lista vacía o null según estándar del proyecto ErrorMessage = ex.Message Meta.Status = "error" Reglas: Usar AppMetaBuilder.Build(...) Agregar metadata extendida si el estándar lo permite: Source = "Catalogo" Cache = true/false RequestId = requestId Alias = defaultDbAlias ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ REPOSITORY ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Crear: ISolicitaListaTiposRespuestaRepository SolicitaListaTiposRespuestaRepository Responsabilidad: Consultar datos. Usar DapperCrudEngine. Usar QueryOptions. Retornar List<ResponseDrowp>. No retornar AppResponses. No manejar cache. No validar claims. No construir metadata. No contener lógica de negocio adicional. Dependencias: IDapperCrudEngine o interfaz equivalente existente en el proyecto ILogger<SolicitaListaTiposRespuestaRepository> si aplica Método sugerido: Task<List<ResponseDrowp>> SolicitaListaTiposRespuestaAsync(string defaultDbAlias); QueryOptions obligatorio: var options = new QueryOptions { TableName = "ra_tipo_respuesta", DefaultAlias = "tr", Columns = new[] { "tr.id_tipo_respuesta AS ID", "tr.descripcion_tipo_respuesta AS Descripcion" }, Filters = new[] { new QueryFilter("tr.estado", 1) }, OrderBy = "tr.orden ASC", Limit = 500 }; Ejecución obligatoria mediante DapperCrudEngine: var resultado = await _DaperCrudEngine.GetAllAsync<ResponseDrowp>( defaultDbAlias, options ); Ajustar la firma exacta según DapperCrudEngine real del proyecto. Reglas: Si DapperCrudEngine retorna wrapper interno, extraer Data según estándar. Si retorna lista directa, retornar lista. Si retorna null, retornar lista vacía. No usar SQL manual. No usar connection.QueryAsync. No usar ExecuteAsync directo. No concatenar strings SQL. ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ DEPENDENCY INJECTION ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Registrar: services.AddScoped<IServiceSolicitaListaTiposRespuesta, ServiceSolicitaListaTiposRespuesta>(); services.AddScoped<ISolicitaListaTiposRespuestaRepository, SolicitaListaTiposRespuestaRepository>(); Si aplica cache: services.AddScoped<ICatalogCacheService, CatalogCacheService>(); No registrar services/repositories como Singleton. Validar que DapperCrudEngine ya esté registrado. Si no existe: services.AddScoped<IDapperCrudEngine, DapperCrudEngine>(); Usar el nombre real de interfaz/clase existente. ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ RESILIENCIA ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Reglas: timeout DB según estándar DapperCrudEngine. retry DB solo si ya existe política centralizada. no implementar retry manual dentro del repository. cache fallback a DB. si cache no responde, continuar con DB. si DB no responde, retornar error controlado. evitar cache stampede. no bloquear indefinidamente por lock de cache. registrar latencia. ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ LOGGING ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Controller INFO: inicio request fin request requestId alias Service INFO: cache hit cache miss consulta DB ejecutada cantidad registros semántica success/empty Service WARNING: cache no disponible cache bypass activo lista vacía si aplica Service ERROR: error DB error inesperado Repository INFO: inicio consulta catálogo fin consulta catálogo cantidad registros Campos mínimos: requestId alias cacheHit totalRegistros duracionMs source = CatalogoTiposRespuesta NO loguear: datos sensibles connection string detalles internos de infraestructura stacktrace al frontend ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ OBSERVABILIDAD ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Métricas recomendadas: catalogo_tipo_respuesta_request_count catalogo_tipo_respuesta_success_count catalogo_tipo_respuesta_empty_count catalogo_tipo_respuesta_error_count catalogo_tipo_respuesta_cache_hit_count catalogo_tipo_respuesta_cache_miss_count catalogo_tipo_respuesta_duration_ms Trazabilidad: requestId obligatorio en logs. alias en logs técnicos. cacheHit en metadata/log. totalRegistros en metadata/log. ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ VALIDACIÓN SOLID ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ SRP: Controller valida entrada HTTP/claims y delega. Service orquesta cache, repository y AppResponses. Repository consulta datos mediante DapperCrudEngine. DTO transporta datos. OCP: Se puede cambiar cache sin modificar repository. Se puede migrar DTO en futuro sin alterar la consulta base. LSP: Interfaces reemplazables por mocks. ISP: Interfaces específicas y pequeñas. DIP: Controller depende de service interface. Service depende de repository interface. Repository depende de DapperCrudEngine abstraction. ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ DEUDA TÉCNICA CONTROLADA ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Documentar: DTO actual ResponseDrowp conserva naming legacy. Futuro recomendado: ResponseDropdownDto . Invalidación avanzada de cache queda como mejora si no existe infraestructura. Lock distribuido para cache stampede queda como mejora si no existe infraestructura. Ruta futura /api/catalogos/tipos-respuesta no se implementa en este ticket salvo decisión explícita. Clasificar deuda: Crítica: ninguna permitida. Media: naming DTO, invalidación avanzada. Menor: ruta futura de catálogo. ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ PRUEBAS OBLIGATORIAS ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Unitarias — Controller: claim defaulalias ausente → BadRequest. claim defaulalias inválido → BadRequest. claim válido → llama service una vez. service Success=true → Ok. service Success=false → BadRequest. no llama repository directamente. logs contienen requestId y alias. Unitarias — Service: alias vacío → AppResponses error. cache hit → no llama repository. cache miss → llama repository. cache falla → llama repository. repository retorna datos → Success true, Message YES. repository retorna vacío → Success true, Message Sin resultados, Meta empty. repository lanza excepción → Success false. guarda cache cuando DB retorna datos. no cachea errores. metadata contiene Cache true/false si aplica. Unitarias — Repository: construye QueryOptions con TableName = ra_tipo_respuesta. usa DefaultAlias = tr. incluye columnas ID y Descripcion. incluye filtro estado = 1. incluye OrderBy tr.orden ASC. incluye Limit = 500. invoca DapperCrudEngine.GetAllAsync<ResponseDrowp>. no usa SQL manual. null result → lista vacía. Integración: endpoint completo con claim válido retorna 200. endpoint sin claim retorna 400. con registros activos retorna lista ordenada. sin registros activos retorna empty. no retorna campos técnicos. cache hit en segunda llamada. cache fallback si Redis/MemoryCache falla. QT / Calidad: validar contrato consumible por frontend. validar formato AppResponses. validar ID y Descripcion. validar que no cambia DTO actual. validar logs sin datos sensibles. validar que repository no retorna AppResponses. validar que no existe SQL manual. validar que se usa DapperCrudEngine. Regresión: no rompe otros catálogos. no cambia ResponseDrowp. no cambia tabla ra_tipo_respuesta. no cambia endpoints existentes. Concurrencia/cache: múltiples requests simultáneos no generan errores. si hay lock cache, solo una llamada DB por cache miss concurrente. si no hay lock, documentar deuda y validar comportamiento seguro. ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ DOCUMENTACIÓN TÉCNICA OBLIGATORIA ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Ruta: Docs/GestionCorrespondencia/TiposRespuesta/ Crear: SCRUM-[ID]-Arquitectura.md Debe incluir: objetivo arquitectónico flujo Controller → Service → Repository diagrama de clases diagrama de secuencia diagrama de estados: request_received claim_validated cache_hit cache_miss db_query success empty error uso obligatorio de DapperCrudEngine uso obligatorio de QueryOptions separación de responsabilidades validación SOLID decisiones técnicas SCRUM-[ID]-Implementacion-Detallada.md Debe incluir: archivos creados rutas exactas controller implementado service implementado repository implementado DTO reutilizado QueryOptions usado DapperCrudEngine usado cache implementado DI agregado manejo de errores metadata generada deuda técnica controlada SCRUM-[ID]-Integracion-Frontend.md Debe incluir: endpoint oficial método HTTP headers/claims requeridos request sin parámetros response esperado ejemplo success ejemplo empty ejemplo error estructura ResponseDrowp recomendaciones para combo/dropdown manejo de lista vacía en UI Ejemplo success: { "success": true, "message": "YES", "data": [ { "id": 1, "descripcion": "Positiva" }, { "id": 2, "descripcion": "Negativa" } ], "meta": { "status": "success" } } Ejemplo empty: { "success": true, "message": "Sin resultados", "data": [], "meta": { "status": "empty" } } SCRUM-[ID]-Pruebas.md Debe incluir: matriz unitarias controller matriz unitarias service matriz unitarias repository matriz integración matriz QT matriz cache matriz regresión criterios de aprobación casos no cubiertos riesgos residuales SCRUM-[ID]-Observabilidad.md Debe incluir: logs por capa campos capturados requestId alias cacheHit totalRegistros duraciónMs métricas recomendadas troubleshooting: claim inválido cache caído DB caído lista vacía error DapperCrudEngine SCRUM-[ID]-Cache.md Debe incluir: estrategia cache-first clave de cache TTL cache por alias serialización fallback a DB invalidación cache stampede deuda técnica si aplica SCRUM-[ID]-Metadata.md Debe incluir: identificador del ticket usuario que creó el ticket fecha módulo tabla relacionada endpoint responsable técnico estado versión observaciones ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ ENTREGABLES ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Código: SolicitaListaTiposRespuestaController.cs IServiceSolicitaListaTiposRespuesta ServiceSolicitaListaTiposRespuesta.cs ISolicitaListaTiposRespuestaRepository SolicitaListaTiposRespuestaRepository.cs Registro DI Configuración cache si aplica Tests unitarios controller Tests unitarios service Tests unitarios repository Tests integración Tests QT Tests cache/concurrencia Documentación: SCRUM-[ID]-Arquitectura.md SCRUM-[ID]-Implementacion-Detallada.md SCRUM-[ID]-Integracion-Frontend.md SCRUM-[ID]-Pruebas.md SCRUM-[ID]-Observabilidad.md SCRUM-[ID]-Cache.md SCRUM-[ID]-Metadata.md ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ CRITERIOS DE ACEPTACIÓN ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Endpoint GET implementado. Claim defaulalias validado. Alias solo desde claim. Controller sin lógica de negocio. Service construye AppResponses. Repository no retorna AppResponses. Repository usa DapperCrudEngine. Repository usa QueryOptions. No existe SQL manual. Consulta tabla ra_tipo_respuesta. Filtra estado = 1. Ordena por orden ASC. Retorna solo ID y Descripcion. Usa ResponseDrowp existente. Implementa cache por alias. Cache tiene TTL. Cache falla sin tumbar endpoint. Empty response controlado. Error response controlado. AppMetaBuilder usado. Logs estructurados. Pruebas unitarias completas. Pruebas integración completas. Pruebas QT completas. Documentación técnica completa. Sin deuda técnica crítica. ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ RESTRICCIONES ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ NO crear DTO nuevo. NO renombrar ResponseDrowp. NO recibir alias por request. NO hardcodear alias. NO SQL manual. NO ExecuteAsync directo. NO QueryAsync directo. NO ExecuteScalarAsync directo. NO AppResponses en repository. NO lógica de negocio en controller. NO lógica de cache en repository. NO exponer campos técnicos. NO cachear errores. NO omitir pruebas. NO omitir documentación. ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ INSTRUCCIÓN FINAL ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Implementar la API SolicitaListaTiposRespuesta respetando: Clean Architecture Controller → Service → Repository DapperCrudEngine obligatorio QueryOptions obligatorio AppResponses estándar seguridad por claim defaulalias cache enterprise pruebas completas documentación técnica detallada El resultado debe quedar listo para consumo frontend y alineado con el estándar backend del proyecto.

## Approach

- Convertir requerimientos del issue en deltas OpenSpec claros y testeables.
- Aplicar restricciones de repositorio, arquitectura y pruebas de OPSXJ_BACKEND_RULES.
- Definir alcance y no-alcance antes de implementar.
- Validar con openspec.cmd validate scrum-173-crea-api-solicita-tipos-respuesta.