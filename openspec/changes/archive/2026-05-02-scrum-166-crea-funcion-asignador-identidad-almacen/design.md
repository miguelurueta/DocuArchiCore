## Context

- Jira issue key: SCRUM-166
- Jira summary: CREA-FUNCION-ASIGNADOR-IDENTIDAD-ALMACENAMIENTO
- Jira URL: https://contasoftcompany.atlassian.net/browse/SCRUM-166

## Context Reference

- openspec/context/multi-repo-context.md
- openspec/context/OPSXJ_BACKEND_RULES.md

## Problem Statement

PROMPT ARQUITECTÓNICO — ORCHESTRATOR / ENGINE PROMPT 5 — IdentityAllocator + SystemStorageRepository + DiskQuotaRepository (ENTERPRISE CRÍTICO — COBERTURA LEGACY SYSTEM1 + DISCO_DETALLE) ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ ROL ESPERADO ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Arquitecto Master Backend .NET especialista en: concurrencia transacciones MySQL SELECT FOR UPDATE DapperCrudEngine QueryOptions control de identidad documental control de disco/capacidad consistencia fuerte sistemas documentales críticos observabilidad transaccional diseño enterprise ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ OBJETIVO ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Extender Prompt 5 para cubrir completamente la lógica legacy relacionada con: reserva de identidad documental en system1 cálculo de disco cálculo de carpeta control de páginas por carpeta validación de tamaño de disco legacy validación de estado/capacidad de disco en disco_detalle preparación para actualización de NUMERO_IMAGENES y NUMPAG_CARP ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ CONTEXTO LEGACY CUBIERTO ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ El legacy realiza: SELECT disco, proxid, tamdisc, numcarp, NUMPAG_CARP FROM system1 WHERE nombre = gabinete FOR UPDATE valida TAMDISC contra valores legacy permitidos valida numcarp != 0 valida disco != 0 valida proxid != 0 valida páginas != 0 consulta disco_detalle para detectar EstadoDisco = "SL" si NUMPAG_CARP + páginas > 230, incrementa carpeta actualiza system1 con proxid, numcarp y NUMPAG_CARP REFERENCIA DE TABLAS Ver: Docs/DataModel/StorageEngine-Tables.md Tablas usadas en este prompt: system1 disco_detalle ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ REGLA GLOBAL OBLIGATORIA — ACCESO A DATOS CON DAPPERCRUDENGINE ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Toda función de repositorio que realice consulta, inserción, actualización, eliminación, bloqueo lógico o lectura transaccional de datos DEBE usar obligatoriamente: DapperCrudEngine QueryOptions QueryFilter / filtros equivalentes del motor modelos tipados de entrada/salida contexto transaccional soportado por el DapperCrudEngine cuando aplique Ruta de referencia obligatoria: D:\imagenesda\GestorDocumental\DocuArchiCore\MiApp.Repository\Repositorio\DataAccess\DapperCrudEngine.cs Regla de arquitectura: Repository = DapperCrudEngine + QueryOptions PROHIBIDO en repositories: connection.ExecuteAsync(...) connection.QueryAsync(...) connection.ExecuteScalarAsync(...) SQL manual concatenado SQL interpolado con datos de usuario construir comandos SQL libres fuera del DapperCrudEngine abrir conexiones directamente abrir transacciones directamente hacer commit o rollback retornar AppResponses<T> resolver claims o lógica HTTP Permitido únicamente si el DapperCrudEngine ya lo soporta o se implementa primero como extensión centralizada: ejecución transaccional mediante DapperCrudEngine QueryOptions con TransactionContext QueryOptions con LockMode = ForUpdate QueryOptions con Insert/Update/Delete tipado QueryOptions con condición de concurrencia QueryOptions con retorno de identidad generada Si el DapperCrudEngine actual NO soporta alguna operación requerida por este prompt, se debe crear primero una extensión centralizada del DapperCrudEngine, documentada y probada, antes de implementar el repository. Queda prohibido saltarse el motor con Dapper directo. ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ REGLA TRANSACCIONAL CON DAPPERCRUDENGINE ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Cuando una operación de repository deba participar en una transacción del Storage Engine: El TransactionCoordinator abre la conexión/transacción o solicita al DapperCrudEngine un contexto transaccional. El repository NO manipula IDbConnection/IDbTransaction directamente salvo que el DapperCrudEngine exponga un contexto transaccional tipado. El repository recibe un contexto de ejecución transaccional, por ejemplo: StorageDbExecutionContext DapperCrudTransactionContext QueryExecutionContext Usar el nombre real disponible en el proyecto. Toda consulta, insert, update, lock o condición de concurrencia debe ejecutarse a través de DapperCrudEngine. Si se requiere SELECT ... FOR UPDATE, debe modelarse mediante QueryOptions.LockMode = ForUpdate o una propiedad equivalente. Si se requiere UPDATE con condición de concurrencia, debe modelarse en QueryOptions con filtros WHERE adicionales. Si se requiere INSERT y retorno de identidad, debe usarse método centralizado del DapperCrudEngine. Si no existe, implementarlo en el motor. ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ DEPENDENCIAS ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Depende de: PROMPT 2 SCRUMCORE-163 — Models PROMPT 4 SCRUMCORE-165 — ValidationPipeline extendido PROMPT 6 SCRUMCORE-167 — TransactionCoordinator ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ UBICACIÓN ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Services: MiApp.Services/Service/GestorDocumental/AlmacenamientoDocumental/Identity/ Repository: MiApp.Repository/Repositorio/GestorDocumental/AlmacenamientoDocumental/SystemStorage/ MiApp.Repository/Repositorio/GestorDocumental/AlmacenamientoDocumental/Disk/ Documentación: Docs/GestorDocumental/AlmacenamientoDocumental/StorageEngine/ Archivos nuevos/adicionales: IStorageIdentityAllocator.cs StorageIdentityAllocator.cs IStorageIdentityPolicy.cs StorageIdentityPolicy.cs ISystemStorageRepository.cs SystemStorageRepository.cs IStorageDiskQuotaRepository.cs StorageDiskQuotaRepository.cs IStorageDiskQuotaPolicy.cs StorageDiskQuotaPolicy.cs ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ MODELOS ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ public sealed class SystemStorageRow { public int Disco { get; init; } public long ProxId { get; init; } public long TamDisc { get; init; } public int NumCarp { get; init; } public int NumPagCarp { get; init; } } public sealed class StorageIdentityReservationResult { public required StorageIdentityModel Identity { get; init; } public long PreviousProxId { get; init; }
public long NewProxId { get; init; }

public int PreviousFolder { get; init; }
public int NewFolder { get; init; }

public int PreviousFolderPages { get; init; }
public int NewFolderPages { get; init; }

public long TamDisc { get; init; } } public sealed class DiskQuotaStatusModel { public int Disco { get; init; } public string NombreGabinete { get; init; } = string.Empty; public string EstadoDisco { get; init; } = string.Empty; public int NumeroImagenes { get; init; } public int NumPagCarp { get; init; } } public sealed class DiskQuotaUpdateModel { public int Disco { get; init; } public string NombreGabinete { get; init; } = string.Empty; public int NuevoNumeroImagenes { get; init; } public int NuevoNumPagCarp { get; init; } } ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ INTERFACES ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ public interface IStorageIdentityAllocator { Task<StorageIdentityReservationResult> ReserveAsync( StorageContext context, IDbConnection connection, IDbTransaction transaction ); } public interface IStorageIdentityPolicy { StorageIdentityReservationResult Calculate( SystemStorageRow current, int numeroPaginasDocumento ); } public interface ISystemStorageRepository { Task<SystemStorageRow?> LockByGabineteAsync( string nombreGabinete, IDbConnection connection, IDbTransaction transaction ); Task<int> UpdateReservationAsync(
    string nombreGabinete,
    StorageIdentityReservationResult reservation,
    IDbConnection connection,
    IDbTransaction transaction
); } public interface IStorageDiskQuotaRepository { Task<DiskQuotaStatusModel?> LockDiskStatusAsync( string nombreGabinete, int disco, IDbConnection connection, IDbTransaction transaction ); Task<int> UpdateDiskUsageAsync(
    DiskQuotaUpdateModel model,
    IDbConnection connection,
    IDbTransaction transaction
); } public interface IStorageDiskQuotaPolicy { void ValidateDiskAvailable(DiskQuotaStatusModel status); } ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ REGLA CRÍTICA LEGACY — PROXID ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Regla legacy real: El código legacy lee PROXID. Luego hace idal = idal + 1. Usa ese idal como IdAlmacen. Actualiza system1.proxid = idal. Por compatibilidad estricta con legacy: IdAlmacen = current.ProxId + 1 NewProxId = IdAlmacen IMPORTANTE: Esta regla difiere de la interpretación alternativa “reservar current.ProxId”. Debe documentarse explícitamente. No cambiar sin prueba de compatibilidad histórica. ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ REGLA LEGACY — TAMDISC ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Valores legacy permitidos: 572523149 4310948432 Regla: si TamDisc no coincide con esos valores, lanzar StorageTransactionException. documentar como regla legacy. si se quiere volver configurable, crear deuda técnica controlada. ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ REGLA LEGACY — PÁGINAS POR CARPETA ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ MaxPaginasPorCarpeta = 230 Regla: if current.NumPagCarp + numeroPaginasDocumento > 230: NewFolder = current.NumCarp + 1 NewFolderPages = numeroPaginasDocumento else: NewFolder = current.NumCarp NewFolderPages = current.NumPagCarp + numeroPaginasDocumento Validar: NumCarp > 0 Disco > 0 ProxId > 0 NumPagCarp >= 0 numeroPaginasDocumento > 0 ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ REGLA LEGACY — DISCO_DETALLE ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Antes de confirmar reserva: consultar disco_detalle con FOR UPDATE. validar EstadoDisco != "SL". si EstadoDisco = "SL", lanzar StorageTransactionException. bloquear fila de disco_detalle durante la transacción. preparar actualización final de NUMERO_IMAGENES y NUMPAG_CARP. ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ AJUSTE ESPECÍFICO PROMPT 5 — SYSTEM1 Y DISCO_DETALLE CON DAPPERCRUDENGINE ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ SystemStorageRepository y StorageDiskQuotaRepository deben implementarse usando DapperCrudEngine. LockByGabineteAsync debe representarse con QueryOptions: var options = new QueryOptions { TableName = "system1", DefaultAlias = "s", Columns = new[] { "s.disco AS Disco", "s.proxid AS ProxId", "s.tamdisc AS TamDisc", "s.numcarp AS NumCarp", "s.NUMPAG_CARP AS NumPagCarp" }, Filters = new[] { new QueryFilter("s.nombre", nombreGabinete) }, Limit = 1, LockMode = QueryLockMode.ForUpdate }; var row = await _DaperCrudEngine.GetFirstOrDefaultAsync<SystemStorageRow>(options, executionContext); UpdateReservationAsync debe representarse con QueryOptions de actualización: var options = new QueryOptions { TableName = "system1", DefaultAlias = "s", UpdateValues = new Dictionary<string, object?> { ["proxid"] = reservation.NewProxId, ["numcarp"] = reservation.NewFolder, ["NUMPAG_CARP"] = reservation.NewFolderPages }, Filters = new[] { new QueryFilter("s.nombre", nombreGabinete), new QueryFilter("s.proxid", reservation.PreviousProxId) } }; var rows = await _DaperCrudEngine.UpdateAsync(options, executionContext); LockDiskStatusAsync debe usar QueryOptions con LockMode = ForUpdate sobre disco_detalle. UpdateDiskUsageAsync debe usar QueryOptions de actualización sobre disco_detalle. Si QueryOptions no tiene UpdateValues o LockMode, agregarlos al DapperCrudEngine como extensión obligatoria antes de implementar este prompt. ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ IMPLEMENTACIÓN — StorageIdentityPolicy ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Responsabilidad: validar SystemStorageRow validar TamDisc legacy calcular IdAlmacen calcular carpeta calcular NUMPAG_CARP retornar StorageIdentityReservationResult Debe incluir: private static readonly long[] AllowedTamDisc = { 572523149, 4310948432 }; private const int MaxPaginasPorCarpeta = 230; Debe lanzar StorageTransactionException ante inconsistencias. ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ IMPLEMENTACIÓN — StorageDiskQuotaPolicy ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Responsabilidad: validar disco_detalle bloquear EstadoDisco = "SL" Reglas: if status == null: throw new StorageTransactionException("Disco no configurado en disco_detalle"); if status.EstadoDisco == "SL": throw new StorageTransactionException($"Disco {status.Disco} sobrepasó el límite de capacidad"); ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ IMPLEMENTACIÓN — SystemStorageRepository ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ LockByGabineteAsync: Debe usar: SELECT disco, proxid, tamdisc, numcarp, NUMPAG_CARP FROM system1 WHERE nombre = @NombreGabinete FOR UPDATE; UpdateReservationAsync: Debe actualizar: proxid numcarp NUMPAG_CARP Debe incluir condición de concurrencia: WHERE nombre = @NombreGabinete AND proxid = @PreviousProxId Si rows != 1 → StorageTransactionException. ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ IMPLEMENTACIÓN — StorageDiskQuotaRepository ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ LockDiskStatusAsync: Debe usar: SELECT disco AS Disco, gabinete AS NombreGabinete, ESTADO_DISCO AS EstadoDisco, NUMERO_IMAGENES AS NumeroImagenes, NUMPAG_CARP AS NumPagCarp FROM disco_detalle WHERE disco = @Disco AND gabinete = @NombreGabinete FOR UPDATE; Nota: Ajustar nombres reales de columnas según schema. Documentar diferencias. UpdateDiskUsageAsync: Debe actualizar: NUMERO_IMAGENES NUMPAG_CARP WHERE disco = @Disco AND gabinete = @NombreGabinete Si rows != 1 → StorageTransactionException. ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ IMPLEMENTACIÓN — StorageIdentityAllocator ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Responsabilidad: Validar context. Validar connection abierta. Validar transaction no null. Lock system1 por gabinete. Calcular reserva. Lock disco_detalle por gabinete/disco. Validar capacidad disco. Update system1. Retornar reserva. NO debe: abrir conexión abrir transacción hacer commit hacer rollback copiar archivos generar XML ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ INTEGRACIÓN CON PROMPT 6 ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Prompt 6 debe recibir también: IStorageDiskQuotaRepository o ejecutar update de disco_detalle vía método coordinado después de inserts DB Regla recomendada: Lock y validación disco_detalle ocurren en Prompt 5 / allocator. Update final de NUMERO_IMAGENES y NUMPAG_CARP puede quedar en Prompt 6 después de que las inserciones DB hayan sido exitosas y antes del commit. ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ DEPENDENCY INJECTION ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ services.AddScoped<IStorageIdentityAllocator, StorageIdentityAllocator>(); services.AddScoped<IStorageIdentityPolicy, StorageIdentityPolicy>(); services.AddScoped<ISystemStorageRepository, SystemStorageRepository>(); services.AddScoped<IStorageDiskQuotaRepository, StorageDiskQuotaRepository>(); services.AddScoped<IStorageDiskQuotaPolicy, StorageDiskQuotaPolicy>(); ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ OBSERVABILIDAD ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Logs obligatorios: INFO: inicio reserva identidad system1 bloqueado disco_detalle bloqueado disco validado idAlmacen generado system1 actualizado WARNING: tamdisc legacy no permitido disco SL conflicto de concurrencia ERROR: system1 no encontrado disco_detalle no encontrado update system1 fallido update disco_detalle fallido Campos: requestId alias usuarioId nombreGabinete disco idAlmacen proxId anterior proxId nuevo carpeta anterior carpeta nueva paginas carpeta anterior paginas carpeta nueva tamdisc estadoDisco duraciónMs ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ PRUEBAS OBLIGATORIAS ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Unitarias — StorageIdentityPolicy: ProxId inválido Disco inválido NumCarp inválido NumPagCarp negativo NumeroPaginasDocumento <= 0 TamDisc inválido TamDisc válido cálculo IdAlmacen = ProxId + 1 cálculo carpeta sin rotación cálculo carpeta con rotación > 230 Unitarias — StorageDiskQuotaPolicy: status null → error EstadoDisco = SL → error EstadoDisco OK → success Integración: SELECT FOR UPDATE system1 bloquea correctamente SELECT FOR UPDATE disco_detalle bloquea correctamente update system1 persiste con commit update system1 revierte con rollback disco SL bloquea operación TamDisc inválido bloquea operación concurrencia con 2+ threads sin duplicidad QT: validar que no hay SQL concatenado validar parámetros Dapper validar logs sin datos sensibles validar compatibilidad con regla legacy de proxid Pruebas adicionales obligatorias por regla DapperCrudEngine: verificar que el repository invoca DapperCrudEngine y no IDbConnection directo. verificar que se construye QueryOptions con TableName correcto. verificar que se usan filtros parametrizados. verificar que LockMode = ForUpdate se usa cuando aplique. verificar que InsertValues/UpdateValues se usan cuando aplique. verificar que no existe ExecuteAsync/QueryAsync/ExecuteScalarAsync directo en el repository. verificar que no existe SQL concatenado. verificar que el contexto transaccional se propaga al DapperCrudEngine. ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ DOCUMENTACIÓN OBLIGATORIA ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Ruta: Docs/GestorDocumental/AlmacenamientoDocumental/StorageEngine/ Crear/actualizar: SCRUM-[ID]-Arquitectura-IdentityAllocator.md Debe incluir: diagrama de clases diagrama de secuencia flujo: lock system1 cálculo identidad lock disco_detalle validación capacidad update system1 explicación de proxid legacy explicación de TamDisc legacy explicación de 230 páginas integración con TransactionCoordinator riesgos de concurrencia validación SOLID SCRUM-[ID]-Implementacion-Detallada-IdentityAllocator.md Debe incluir: archivos creados rutas exactas métodos implementados SQL conceptual lógica de cálculo regla proxid legacy regla tamdisc legacy regla disco_detalle control de concurrencia decisiones técnicas deuda técnica SCRUM-[ID]-Pruebas-IdentityAllocator.md Debe incluir: matriz pruebas unitarias matriz integración matriz concurrencia matriz QT pruebas proxid pruebas tamdisc pruebas disco SL pruebas rollback/commit SCRUM-[ID]-Observabilidad-IdentityAllocator.md Debe incluir: logs campos capturados métricas recomendadas troubleshooting: system1 no encontrado disco_detalle no encontrado TamDisc inválido disco SL concurrencia proxid SCRUM-[ID]-Cobertura-Legacy-IdentityAllocator.md Debe incluir: tabla legacy → nuevo componente system1 FOR UPDATE → SystemStorageRepository TamDisc → StorageIdentityPolicy EstadoDisco SL → StorageDiskQuotaPolicy disco_detalle → StorageDiskQuotaRepository NUMPAG_CARP → StorageIdentityPolicy proxid legacy → StorageIdentityPolicy SCRUM-[ID]-Concurrencia-IdentityAllocator.md Debe incluir: estrategia de locking orden de bloqueo system1 → disco_detalle riesgos deadlock mitigaciones pruebas multi-thread SCRUM-[ID]-Metadata.md Debe incluir: ticket autor fecha relación Prompt 4 relación Prompt 6 estado Documentar adicionalmente por regla DapperCrudEngine: método del DapperCrudEngine usado. QueryOptions construido. filtros aplicados. columnas explícitas. LockMode / InsertValues / UpdateValues usados. cómo se propaga el contexto transaccional. extensiones agregadas al DapperCrudEngine si fueron necesarias. evidencia de que no se usa Dapper directo ni SQL manual. ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ CRITERIOS DE ACEPTACIÓN ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ SystemStorageRepository implementado. StorageIdentityAllocator implementado. StorageIdentityPolicy implementado. StorageDiskQuotaRepository implementado. StorageDiskQuotaPolicy implementado. ProxId legacy respetado. TamDisc legacy validado. DiscoDetalle validado. EstadoDisco SL bloquea. NUMPAG_CARP calculado. Carpeta rota sobre 230. Concurrencia controlada. SELECT FOR UPDATE aplicado. DI completo. Pruebas completas. Documentación completa. Listo para Prompt 6 extendido. ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ RESTRICCIONES ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ NO abrir conexión. NO abrir transacción. NO hacer commit. NO hacer rollback. NO acceder filesystem. NO generar XML. NO insertar gabinete. NO insertar inventario. NO log workflow. NO usar Dapper directo desde repositories. NO usar IDbConnection.ExecuteAsync / QueryAsync / ExecuteScalarAsync desde repositories. NO omitir DapperCrudEngine. NO implementar SQL manual si puede modelarse con QueryOptions. NO usar SQL directo para FOR UPDATE; usar LockMode o extensión centralizada del DapperCrudEngine. NO usar SQL directo para INSERT/UPDATE; usar InsertValues/UpdateValues o extensión centralizada del DapperCrudEngine. ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ INSTRUCCIÓN FINAL ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Extender Prompt 5 para cubrir system1 + disco_detalle del legacy, manteniendo transacción externa, locking fuerte, Dapper parametrizado, pruebas y documentación completa.

## Approach

- Convertir requerimientos del issue en deltas OpenSpec claros y testeables.
- Aplicar restricciones de repositorio, arquitectura y pruebas de OPSXJ_BACKEND_RULES.
- Definir alcance y no-alcance antes de implementar.
- Validar con openspec.cmd validate scrum-166-crea-funcion-asignador-identidad-almacen.