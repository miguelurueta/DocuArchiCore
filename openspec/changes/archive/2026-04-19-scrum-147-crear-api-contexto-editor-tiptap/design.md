## Context

- Jira issue key: SCRUM-147
- Jira summary: CREAR-API-CONTEXTO-EDITOR-TIPTAP
- Jira URL: https://contasoftcompany.atlassian.net/browse/SCRUM-147

## Context Reference

- openspec/context/multi-repo-context.md
- openspec/context/OPSXJ_BACKEND_RULES.md

## Problem Statement

PROMPT ARQUITECTÓNICO — Ticket BE Crear API + Service + Repository: Persistencia de contexto de documento editor usando catálogo de contextos (MISMA API) Rol esperado Arquitecto backend senior (.NET, Core, Dapper, modelado transversal, integridad de datos, pruebas y documentación). ——— OBJETIVO Implementar la persistencia de la relación entre un documento del editor Tiptap y una entidad de negocio del sistema usando un catálogo formal de contextos configurables. Esta versión elimina entity_name y relation_type como texto libre en el request y los reemplaza por: ContextCode (entrada del request) ra_editor_context_definitions (catálogo) context_definition_id (referencia persistida) La solución debe: evitar strings inconsistentes controlar contextos activos/inactivos permitir evolución enterprise del editor a múltiples módulos ——— CONTEXTO EXISTENTE ra_editor_documents almacena HTML del editor. ra_editor_document_images almacena imágenes. ra_editor_document_image_links relaciona documento ↔ imágenes. Se requiere una capa transversal para vincular documento ↔ negocio. Decisión: crear catálogo ra_editor_context_definitions. Decisión: ra_editor_document_context ya no debe persistir entity_name/relation_type repetidos; debe persistir context_definition_id. ——— OBJETIVO FUNCIONAL Registrar una relación entre: document_id (editor) context_definition_id (desde catálogo, resuelto por ContextCode) entity_id (id real del negocio) Ejemplo: Catálogo: ContextCode=RAD_GESTION_RESPUESTA (entity_name=rad_gestion, relation_type=documento_respuesta) Relación: document_id=150, context_definition_id=1, entity_id=911 ——— UBICACIÓN ESPERADA Controller: DocuArchi.Api/Controllers/GestorDocumental/Editor/GuardaEditorDocumentContextController.cs Service: MiApp.Services/Service/GestorDocumental/Editor/ServiceGuardaEditorDocumentContext.cs Repositorio (contexto): MiApp.Repository/Repositorio/GestorDocumental/Editor/GuardaEditorDocumentContextRepository.cs Repositorio (catálogo): MiApp.Repository/Repositorio/GestorDocumental/Editor/SolicitaEditorContextDefinitionRepository.cs Modelos: MiApp.Models/Models/GestorDocumental/Editor/RaEditorDocumentContext.cs MiApp.Models/Models/GestorDocumental/Editor/RaEditorContextDefinition.cs DTO: MiApp.DTOs/DTOs/GestorDocumental/Editor/ Docs: Docs/GestorDocumental/Editor/ ——— TABLAS INVOLUCRADAS Catálogo: ra_editor_context_definitions Campos mínimos relevantes: context_definition_id (PK) context_code (único, requerido) entity_name (requerido) relation_type (requerido) module_name description is_active allow_multiple_documents allow_multiple_contexts_per_document requires_unique_entity created_by created_at Relacional: ra_editor_document_context Campos mínimos relevantes: context_id (PK) document_id context_definition_id entity_id is_active created_by created_at deactivated_by deactivated_at ——— MODELOS OFICIALES Usar los modelos propuestos con [Column("snake_case")] para asegurar mapping consistente. ——— CONTRATO OFICIAL Request DTO public sealed class GuardaEditorDocumentContextDto { public long DocumentId { get; set; } public string ContextCode { get; set; } = null!; public long EntityId { get; set; } public string? CreatedBy { get; set; } } Response AppResponses<RaEditorDocumentContext?> Semántica: success=true y data!=null → relación creada o reutilizada (idempotente) success=false y data=null → error controlado errors según estándar ——— RUTA OFICIAL (MISMA API) Base: api/gestor-documental/editor Endpoint: POST /api/gestor-documental/editor/document/context Nota: Endpoint técnico y reutilizable. Debe existir desacoplado aunque Full Save lo consuma internamente. ——— REGLAS DE SEGURIDAD (OBLIGATORIAS) Validar claim defaulalias Validar request Validar DocumentId > 0 Validar ContextCode requerido (Trim, no whitespace) Validar EntityId > 0 Prohibido registrar contexto con: documento inexistente ContextCode inexistente ContextCode inactivo (is_active=0) No exponer lógica específica de un solo módulo ——— IMPLEMENTACIÓN — CONTROLLER Nombre: GuardaEditorDocumentContextController Responsabilidades: Validar claim defaulalias Validar request nulo Validar DocumentId > 0 Validar ContextCode (no null/empty/whitespace) Validar EntityId > 0 Invocar service success=false → BadRequest(result) success=true → Ok(result) Restricciones: No consultar repositorios directo No resolver catálogo en controller No SQL/joins en controller ——— IMPLEMENTACIÓN — SERVICE Interface: public interface IServiceGuardaEditorDocumentContext { Task<AppResponses<RaEditorDocumentContext?>> GuardaEditorDocumentContextAsync( GuardaEditorDocumentContextDto request, string defaultDbAlias ); } Responsabilidades: Validar defensivamente defaultDbAlias (Trim, requerido) Validar DocumentId > 0 Validar ContextCode requerido, normalizar con Trim() Validar EntityId > 0 Resolver definición activa desde catálogo por ContextCode Aplicar reglas del catálogo (ver sección “Reglas del catálogo”) Invocar repositorio de contexto (persistencia) try/catch y retorno estándar AppResponses<RaEditorDocumentContext?> Restricciones: No acceder directo a BD (solo vía repos) No guardar documento No sincronizar imágenes No convertir este caso de uso en Full Save ——— IMPLEMENTACIÓN — REPOSITORIO DE CATÁLOGO Interface: public interface ISolicitaEditorContextDefinitionRepository { Task<AppResponses<RaEditorContextDefinition?>> SolicitaPorContextCodeAsync( string contextCode, string defaultDbAlias, IDbConnection? conn = null, IDbTransaction? trans = null ); } Responsabilidades: Buscar por context_code Exigir is_active = 1 Retornar definición encontrada Semántica: Si no existe → success=false, data=null (Validation: “ContextCode inválido”) Si existe pero inactivo → success=false, data=null (Validation: “ContextCode inactivo”) Reglas: No SQL concatenado (solo parámetros) Soportar conn/trans externas (para Full Save) ——— IMPLEMENTACIÓN — REPOSITORIO DE CONTEXTO Interface: public interface IGuardaEditorDocumentContextRepository { Task<AppResponses<RaEditorDocumentContext?>> GuardaEditorDocumentContextAsync( long documentId, long contextDefinitionId, long entityId, string? createdBy, string defaultDbAlias, IDbConnection? conn = null, IDbTransaction? trans = null ); } Responsabilidades: Validar existencia de document_id en ra_editor_documents Persistir relación en ra_editor_document_context Implementar idempotencia (no duplicar relación activa equivalente) Soportar transacción externa (conn/trans) Retornar la relación persistida o existente Reglas críticas de persistencia: Idempotencia / duplicado activo No crear nueva fila si ya existe relación activa con: mismo document_id mismo context_definition_id mismo entity_id is_active = 1 En ese caso retornar la existente (success=true). No borrar histórico No desactivar ni borrar relaciones previas en este ticket (solo insert o reuse). is_active controla vigencia Inserciones nuevas deben crear is_active = 1, created_at = now. No SQL concatenado manualmente (solo parámetros). ——— REGLAS DEL CATÁLOGO (OBLIGATORIAS, SE APLICAN EN SERVICE) Estas reglas se validan antes de persistir: requires_unique_entity Si true: No debe existir otra relación activa para el mismo: context_definition_id entity_id apuntando a un documento distinto (o a cualquier documento, según la política definida). Recomendado (más estricto y útil): “solo puede existir 1 documento activo por entidad en ese contexto”. allow_multiple_contexts_per_document Si false: No permitir más de 1 relación activa para el mismo document_id dentro del mismo context_definition_id (o incluso para cualquier contextDefinition, según decisión). Recomendado (acotado): aplica solo dentro del mismo context_definition_id. allow_multiple_documents Si false: No permitir que una entidad (entity_id) tenga múltiples documentos activos en el mismo context_definition_id. (Se puede cubrir con requires_unique_entity; si ambos existen, documentar precedencia.) Nota: si alguna regla no se implementa completa en esta fase, debe documentarse como deuda técnica explícita y no marcar el ticket como Done. ——— ESTRATEGIA DE PERSISTENCIA (RESUMEN) Validaciones request + alias Resolver context_definition_id desde ContextCode (activo) Validar existencia del documento Aplicar reglas del catálogo Buscar duplicado activo equivalente Insertar o retornar existente (idempotente) Retornar AppResponses<RaEditorDocumentContext?> ——— VALIDACIONES OBLIGATORIAS defaultDbAlias: requerido, Trim DocumentId: requerido, > 0 ContextCode: requerido, Trim, no whitespace EntityId: requerido, > 0 No insertar si: ContextCode no existe ContextCode inactivo documento no existe viola reglas del catálogo es duplicado activo ——— SEMÁNTICA DE RESPUESTA (OBLIGATORIA) Creada: success=true, data!=null, errors=[] Existente (idempotente): success=true, data!=null, errors=[] Documento inexistente: success=false, data=null, errors Validation ContextCode inexistente/inactivo: success=false, data=null, errors Validation Violación reglas catálogo: success=false, data=null, errors Validation (mensaje claro) Excepción: success=false, data=null, errors Exception ——— INFRAESTRUCTURA DE DATOS (PARA SER COMPATIBLE CON EL MÓDULO EDITOR) Usar la infraestructura existente del proyecto: Dapper + IDbConnectionFactory (y/o engine existente si aplica). No forzar IDapperCrudEngine si el patrón del módulo Editor no lo usa para este caso. Todo SQL debe ser parametrizado (sin concatenación). ——— PRUEBAS OBLIGATORIAS Controller unit claim defaulalias ausente → 400 request null → 400 DocumentId <= 0 → 400 ContextCode null/whitespace → 400 EntityId <= 0 → 400 relación nueva → 200 relación existente → 200 error del service → 400 Service unit alias inválido → error controlado parámetros inválidos → error controlado ContextCode inexistente → error controlado ContextCode inactivo → error controlado viola reglas del catálogo → error controlado relación nueva → success true relación existente → success true repo throw → error controlado (Exception) Repo catálogo encuentra ContextCode activo no encuentra ContextCode inexistente no retorna ContextCode inactivo soporta conn/trans externas Repo contexto valida existencia documento inserta relación nueva idempotencia (no duplica activo) soporta conn/trans externas Integración (preferida con MySQL Testcontainers/Docker; si no, Skip explícito) lookup real en ra_editor_context_definitions insert real en ra_editor_document_context validación real de existencia en ra_editor_documents idempotencia frente a reintento validación de reglas del catálogo (mínimo requires_unique_entity) ——— DOCUMENTACIÓN OBLIGATORIA (Docs/GestorDocumental/Editor/) SCRUM-[ID]-Integracion-Frontend-GuardaEditorDocumentContext.md (request usa ContextCode) SCRUM-[ID]-Arquitectura-GuardaEditorDocumentContext.md (catálogo + secuencia) SCRUM-[ID]-Implementacion-Detallada-GuardaEditorDocumentContext.md (incluye reglas del catálogo) SCRUM-[ID]-Pruebas-GuardaEditorDocumentContext.md Actualizar docs existentes (si aplica) para reflejar: ContextCode como entrada oficial resolución interna context_definition_id integración con Full Save ——— ENTREGABLES Código: Controller + Service + 2 repos (catálogo/contexto) + modelos + DTOs + DI DB: scripts ra_editor_context_definitions y ra_editor_document_context + índices recomendados Docs: 4 documentos estándar + actualización de relacionados ——— CRITERIOS DE ACEPTACIÓN ra_editor_context_definitions existe y controla ContextCode (activo/inactivo) ra_editor_document_context persiste context_definition_id (no strings libres) Endpoint POST /api/gestor-documental/editor/document/context se mantiene (misma ruta) Valida claim defaulalias y request No permite ContextCode inexistente/inactivo No permite documento inexistente Aplica reglas del catálogo (mínimo requires_unique_entity) y documenta las demás Idempotencia: no duplica relaciones activas equivalentes Soporta conn/trans externas Mantiene AppResponses<RaEditorDocumentContext?> Pruebas mínimas implementadas + documentación completa

## Approach

- Convertir requerimientos del issue en deltas OpenSpec claros y testeables.
- Aplicar restricciones de repositorio, arquitectura y pruebas de OPSXJ_BACKEND_RULES.
- Definir alcance y no-alcance antes de implementar.
- Validar con openspec.cmd validate scrum-147-crear-api-contexto-editor-tiptap.