## Context

- Jira issue key: SCRUM-146
- Jira summary: ACTUALIZAR-GUARDAR-DOCUMENTO-REPOSITORIO
- Jira URL: https://contasoftcompany.atlassian.net/browse/SCRUM-146

## Context Reference

- openspec/context/multi-repo-context.md
- openspec/context/OPSXJ_BACKEND_RULES.md

## Problem Statement

PROMPT ARQUITECTÓNICO — Ticket BE (CORREGIDO / MÁS TESTEABLE) Ajustar GuardaEditorDocumentRepository como repositorio base reutilizable (NO orquestador) Rol esperado Arquitecto backend senior (.NET, Dapper, transacciones, Clean Architecture, SOLID) ——— OBJETIVO Ajustar el repositorio actual GuardaEditorDocumentRepository para: Mantenerlo como repositorio puro (Ticket 1). Prepararlo para ser reutilizado por el orquestador Full Save (Ticket 4) mediante conn/trans. Corregir inconsistencias técnicas. Garantizar integridad de datos en insert/update. ——— CAMBIOS REQUERIDOS 1) NO convertir este repositorio en orquestador Prohibido agregar: sincronización de imágenes llamadas a otros repositorios (excepto consultas mínimas de existencia del mismo documento) lógica de negocio o reglas del caso de uso Full Save El repositorio solo persiste/consulta ra_editor_documents. 2) Corregir manejo de fechas (regla obligatoria) INSERT created_at = now updated_at = now UPDATE NO modificar created_at solo updated_at = now Requisito adicional: el código no debe setear CreatedAt = now en el flujo de update (evitar inconsistencias futuras). 3) Validar existencia en UPDATE (obligatorio) Si DocumentId > 0: Verificar que el documento existe antes de reportar éxito. Si no existe ⇒ retornar error controlado (no excepción no manejada). Se aceptan 2 enfoques (elige 1 y documenta): A. Pre-check: SELECT 1/created_at FROM ra_editor_documents WHERE document_id=@id dentro de la misma transacción. B. RowsAffected: ejecutar UPDATE ... WHERE document_id=@id y si rowsAffected == 0 ⇒ error “no existe”. 4) Consistencia de nombres de columnas (modelo y SQL) Asegurar que el mapping sea consistente: document_id ↔ DocumentId template_id ↔ TemplateId created_at ↔ CreatedAt etc. Evitar depender de SELECT * si no hay garantía explícita de mapping (atributos [Column] o convención global confirmada). Recomendación: seleccionar columnas explícitas o aliasar para garantizar el binding. 5) Mantener soporte de transacción externa (obligatorio) La firma debe seguir soportando: IDbConnection? conn IDbTransaction? trans Reglas: Si conn viene externo ⇒ no crear, no commit/rollback y no dispose. Si conn es null ⇒ el repo puede crear conn/trans locales y debe encargarse de commit/rollback/dispose. ——— REGLAS (SRP / Clean / SOLID) Repositorio = “persistencia pura” + validaciones mínimas de integridad técnica. No introducir flujos “Full Save”. No agregar sincronización de imágenes. No agregar contexto de negocio. Mantener cohesión alta y acoplamiento bajo. ——— PRUEBAS (obligatorias y alineadas a Dapper/SQL real) Cobertura mínima: Insert correcto created_at y updated_at quedan seteados. Update correcto created_at no cambia updated_at sí cambia Update sin existencia retorna error controlado “Documento no existe” (sin throw no manejado) Notas: Preferido: Integration test (MySQL Testcontainers/Docker) por ser SQL real. Si Docker no está disponible: dejar el integration test en Skip con mensaje explícito y, opcionalmente, unit tests para validaciones (alias, html requerido, etc.). ——— DOCUMENTACIÓN (ACTUALIZAR) Actualizar el documento de implementación detallada de Guardar documento (archivo existente en Docs/GestorDocumental/ Editor/): Agregar sección: “Uso como repositorio base dentro del Full Save” Explicar que el orquestador invoca este repo pasando conn/trans. Explicitar que el repo no orquesta ni sincroniza imágenes. Incluir mini-diagrama o bullets de flujo: Orquestador → Repo (documents) → (otros repos por fuera del scope del repo). ——— CRITERIOS DE ACEPTACIÓN Repositorio NO orquesta (SRP intacto). Fechas correctas (insert/update). Validación de existencia implementada para update. Column mapping consistente y explícito (no ambiguo). Soporte conn/trans preservado. Documentación actualizada con uso como “repo base” dentro de Full Save.

## Approach

- Convertir requerimientos del issue en deltas OpenSpec claros y testeables.
- Aplicar restricciones de repositorio, arquitectura y pruebas de OPSXJ_BACKEND_RULES.
- Definir alcance y no-alcance antes de implementar.
- Validar con openspec.cmd validate scrum-146-actualizar-guardar-documento-repositorio.