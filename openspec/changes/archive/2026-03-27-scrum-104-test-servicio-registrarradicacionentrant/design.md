## Context

- Jira issue key: SCRUM-104
- Jira summary: TEST-SERVICIO-RegistrarRadicacionEntranteAsync
- Jira URL: https://contasoftcompany.atlassian.net/browse/SCRUM-104

## Context Reference

- openspec/context/multi-repo-context.md
- openspec/context/OPSXJ_BACKEND_RULES.md

## Problem Statement

PROMPT ARQUITECTÓNICO — Diseño de pruebas para RegistrarRadicacionEntranteAsync Rol esperado: Arquitecto de software senior, QA técnico y desarrollador backend .NET OBJETIVO Definir, diseñar e implementar el conjunto de pruebas necesario para validar el servicio RegistrarRadicacionEntranteAsync dentro de DocuArchiCore, cubriendo: reglas de negocio orquestación de dependencias integración con repositorios flujo workflow atomicidad manejo de errores compatibilidad contractual Las pruebas deben asegurar que el servicio se comporte correctamente en escenarios exitosos, escenarios sin datos, escenarios con validaciones funcionales y escenarios de falla técnica. ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ ALCANCE ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ El análisis y la implementación de pruebas debe cubrir: RegistrarRadicacionEntranteAsync repositorios consumidos por el servicio servicios auxiliares consumidos por el flujo claims usados por el flujo DTOs de request y response impactados reglas de continuidad o interrupción del proceso registro de tarea workflow cuando aplica no duplicación de tarea workflow cuando ya existe ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ ARQUITECTURA DE PRUEBAS ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Las pruebas deben dividirse en: Unit Tests Integration Tests Contract Tests si aplica Regression Tests ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ BLOQUE 1 — PRUEBAS UNITARIAS DEL SERVICIO ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Diseñar pruebas unitarias para validar la orquestación de RegistrarRadicacionEntranteAsync con dependencias mockeadas. Escenarios mínimos obligatorios: A. Flujo base exitoso registra correctamente cuando todos los datos son válidos retorna AppResponses exitoso ejecuta dependencias en el orden esperado B. Validaciones de entrada request null DTO principal null campos obligatorios faltantes valores inválidos o en cero estructuras anidadas null C. Flujo de plantilla usa correctamente SolicitaEstructuraPlantillaRadicacionDefault retorna error controlado cuando no existe plantilla maneja error técnico del repositorio de plantilla D. Flujo workflow no ejecuta lógica workflow cuando no aplica ejecuta lógica workflow cuando sí aplica obtiene defaultDbAlias desde claims falla de forma controlada si no existe claim requerido falla si no existe ruta workflow falla si no existe usuario workflow falla si no existe actividad workflow relacionada al grupo no registra tarea workflow si ya existe registra tarea workflow cuando no existe y aplica detiene el flujo si falla la validación workflow detiene el flujo si falla el registro de tarea workflow E. Reglas de continuidad continúa si una consulta no bloqueante retorna sin datos detiene si una consulta obligatoria retorna sin datos distingue entre: success true sin datos success false excepción técnica F. Manejo de errores captura excepciones de dependencias retorna respuesta controlada no deja fallos sin manejar ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ BLOQUE 2 — PRUEBAS UNITARIAS DE LÓGICA AUXILIAR ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Diseñar pruebas unitarias para los componentes auxiliares consumidos por el servicio, si existen. Escenarios sugeridos: asignación de datos workflow validación de campos workflow construcción de nombreRuta reglas de correspondencia entre request y estructura workflow validaciones de longitud y formato condiciones de interrupción del flujo ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ BLOQUE 3 — PRUEBAS DE INTEGRACIÓN ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Diseñar pruebas de integración reales con infraestructura para validar el comportamiento del flujo completo. Usar: MySQL Testcontainers / Docker Escenarios mínimos obligatorios: A. Registro real exitoso inserta correctamente el radicado persiste datos coherentes en tablas reales B. Flujo workflow real consulta correctamente ruta workflow consulta correctamente usuario workflow consulta correctamente actividad relacionada al grupo registra correctamente la tarea workflow cuando aplica no registra tarea workflow cuando no aplica no duplica tarea workflow si ya existe C. Atomicidad rollback si falla el registro de tarea workflow rollback si falla una inserción intermedia crítica no deja datos parciales inconsistentes D. Claims y alias usa correctamente defaultDbAlias desde claims falla de forma controlada si el alias no existe o es inválido E. Consultas reales comportamiento con registros válidos comportamiento sin registros comportamiento con relaciones incompletas comportamiento con tablas dinámicas válidas comportamiento con datos inválidos de workflow ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ BLOQUE 4 — PRUEBAS CONTRACTUALES ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Si el servicio es consumido por endpoint o frontend, validar contratos. Escenarios mínimos: request DTO coincide con contrato actual response mantiene AppResponses atributos eliminados realmente no están presentes no se rompen consumidores actuales sin justificación el contrato de salida sigue siendo consistente ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ BLOQUE 5 — PRUEBAS DE REGRESIÓN ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Diseñar un set de regresión para proteger el flujo principal de radicación. Escenarios obligatorios: radicación normal sin workflow radicación con workflow radicación con datos mínimos válidos radicación con datos máximos válidos radicación con error de plantilla radicación con error de usuario workflow radicación con error de actividad workflow radicación cuando ya existe tarea workflow radicación cuando una consulta auxiliar retorna sin datos y el flujo debe seguir ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ BLOQUE 6 — PRUEBAS DE BORDE ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Diseñar escenarios edge case para robustez del servicio. Escenarios sugeridos: tipoModuloRadicacion en valores límite id_tipo_flujo_workflow = 0 Grupos_Workflow_Id_Grupo = 0 strings vacíos, null y espacios fechas fuera de rango listas vacías columnas dinámicas inexistentes alias vacíos claims ausentes IDs muy grandes caracteres especiales en campos de entrada ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ CRITERIOS TÉCNICOS ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Mantener AppResponses<T> como contrato estándar Usar mocks para pruebas unitarias No usar infraestructura real en unit tests Usar infraestructura aislada en integration tests Mantener las pruebas legibles, pequeñas y deterministas Nombrar pruebas según comportamiento observado No mezclar pruebas unitarias con integración Cubrir escenarios positivos, negativos y de borde ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ CRITERIOS DE ACEPTACIÓN ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Existe cobertura unitaria del flujo principal de RegistrarRadicacionEntranteAsync Existe cobertura del flujo workflow cuando aplica Existe cobertura de errores técnicos y errores funcionales Existen pruebas de integración para persistencia real y rollback El servicio queda protegido contra regresiones críticas Las pruebas documentan claramente cuándo el flujo continúa y cuándo se interrumpe Documentar todas las pruebas Docs/Radicacion/RegistroRadicado/Pruebas/ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ ENTREGABLES ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Suite de pruebas unitarias para RegistrarRadicacionEntranteAsync Suite de pruebas unitarias para lógica auxiliar impactada Suite de pruebas de integración con MySQL/Testcontainers Casos de prueba de regresión documentados Casos de prueba contractuales si aplica Documentación técnica de cobertura y escenarios cubiertos ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ RESTRICCIONES ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ No cambiar la lógica funcional del servicio solo para facilitar pruebas No romper contratos existentes No mezclar escenarios de infraestructura en unit tests No omitir validaciones críticas del flujo workflow No asumir que “sin datos” siempre significa error; respetar la regla funcional de cada consulta

## Approach

- Convertir requerimientos del issue en deltas OpenSpec claros y testeables.
- Aplicar restricciones de repositorio, arquitectura y pruebas de OPSXJ_BACKEND_RULES.
- Definir alcance y no-alcance antes de implementar.
- Validar con openspec.cmd validate scrum-104-test-servicio-registrarradicacionentrant.